spring:
  datasource:
    url: jdbc:h2:mem:babytalk;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  api:
    current-version: 1.0.0
    min-supported-version: 1.0.0