name: baby_talk_worktree_root
description: "Worktree root — flutter test test/smoke/ from root delegates to mobile/"
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  mobile:
    path: mobile
  test: any

dependency_overrides:
  win32: 5.15.0

flutter:
  uses-material-design: true
  assets:
    - mobile/assets/content/seed_content.json
    - mobile/assets/audio/phrases/
