# Integrations

This directory is reserved for project-local governance integration notes.

Use it for stable adapter rules such as gstack, Superpowers, code-analysis, CEO plan import, and other external workflows.

Do not place one-off execution logs here; use `ai/runtime/` for runtime noise and `ai/context/` for formal evidence.