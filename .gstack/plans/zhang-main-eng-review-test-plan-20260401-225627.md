# Test Plan v3 (Complete Coverage)
Generated by /plan-eng-review on 2026-04-01
Branch: main
Repo: zhangspaghetti/baby-talk-2

## Tech Stack
- Frontend: Flutter (Dart) — flutter_test + integration_test
- Backend: Spring Boot 4 + JDK 21 — JUnit 5 + MockMvc + Testcontainers (PostgreSQL + pgvector + Redis)
- State management: Provider + ChangeNotifier
- Auth: JWT + Refresh Token
- Deploy: Docker container (local dev → 阿里云 ECS)

## Affected Screens/Routes

### Flutter Screens
- Onboarding (login + child name + birthdate + PIPL consent)
- Smart Home (phrase of day, recommendations, nudge, Ask Coach)
- Scene Coaching (phrase cards, reactions, couple mode, pronunciation)
- Celebration Screen (first babble animation, share)
- Progress (stats, roadmap, mastery, milestones)
- Journey (voice memo diary, native-only)
- AI Ask Coach (text + voice input, streaming response)

### API Endpoints
- POST /api/v1/auth/sms — Send verification code
- POST /api/v1/auth/verify — Verify SMS code → JWT
- POST /api/v1/auth/login — Username/password login
- POST /api/v1/auth/refresh — Refresh token
- POST /api/v1/sync/events — Batch upload interaction events
- GET /api/v1/sync/progress — Get user progress
- POST /api/v1/sync/progress — Update user progress
- POST /api/v1/coach/ask — AI phrase request (text → RAG → LLM)
- POST /api/v1/coach/feedback — Thumbs up/down
- GET /api/v1/coach/tts — On-demand TTS audio
- GET /api/v1/admin/pending — List pending reviews
- POST /api/v1/admin/approve — Approve phrase
- POST /api/v1/admin/discard — Discard phrase

## Key Interactions to Verify

### Auth (NEW gaps from v3)
- Invalid phone number format → validation error
- SMS send failure (阿里云 down) → clear error + retry
- Wrong SMS code 3x → rate limit lockout for 15min
- SMS code expiry after 5min → expired message + resend
- Concurrent SMS requests (spam tap) → debounce, only one sent
- Wrong password 5x → account lockout for 30min
- Access token expired mid-request → auto-refresh via interceptor
- Refresh token expired → force re-login screen
- Multiple devices same account → second device login OK, first device token still valid until expiry

### Onboarding (NEW)
- PIPL consent flow shown before any data collection
- PIPL consent declined → cannot proceed, explain why
- PIPL consent accepted → stored locally, synced to server

### Scene Coaching (existing + NEW)
- Phrase card swipe gesture between phrases (NEW)
- Chinese fallback when baby is crying (NEW)
- Screen orientation stays portrait in couple mode (NEW)
- All existing v1+v2 tests still apply

### Smart Home (existing + NEW)
- Phone timezone change mid-day → phrase of day may refresh or stay (NEW)
- No activities for current stage → "more content coming soon" (NEW)
- All existing v1+v2 tests still apply

### AI Ask Coach (MAJOR NEW COVERAGE)
- Empty input submission → validation error, no API call
- Input > 500 chars → truncation warning
- RAG similarity search: match found (>0.90) → return pre-approved phrase, no LLM call
- RAG similarity search: no match → LLM fallback, log to pending review
- RAG cold-start (<500 entries) → higher threshold (0.90), verified
- LLM response quality eval: parenting methodology compliance [→EVAL]
- Client-side profanity filter: inappropriate input blocked
- Client-side profanity filter: false positive (legitimate parenting term flagged)
- ASR → text → LLM → response → TTS full E2E flow [→E2E]
- ASR confidence < 70% → automatic fallback to text input
- Noisy environment (baby crying) → ASR failure → text fallback
- Rate limit counter reset at UTC midnight
- Rate limit: 10th request succeeds, 11th shows limit message
- TTS generation timeout (>5s) → show text-only result

### Pronunciation Comparison (NEW)
- Mic permission denied → explanation dialog + settings link
- Recording too short (<1s) → "try again" prompt
- Recording too long (>30s) → auto-stop

### Voice Memo Diary (NEW)
- 30-second auto-stop at boundary
- OSS upload failure → retry queue with exponential backoff
- Device storage full → warning + offer to delete oldest
- Sunday 6pm prompt trigger

### Local Push Notifications (NEW)
- flutter_local_notifications initialization
- 6pm contextual phrase selection algorithm
- Alarm permission denied on Android 12+ → graceful fallback
- Notification tapped → opens correct scene

### Sync Service (NEW)
- Long offline period (1000+ events queued) → batch upload
- Sync conflict (unlikely) → server wins for progress, client wins for events

### Admin Review Page (NEW)
- List pending → show all PhraseRequests with was_helpful null
- Approve → add to knowledge base with embedding
- Discard → remove from pending, don't add to KB

## Edge Cases

### Auth Edge Cases
- Phone number with country code prefix → strip and validate
- SMS code: leading zeros preserved (code "001234" != "1234")
- JWT token tampered → 401 unauthorized
- Clock skew between device and server > 5min → JWT validation fails gracefully

### Coaching Edge Cases
- All phrases in all scenes at all difficulties mastered → "complete!" state
- Scene with no phrases at current difficulty → "coming soon" card
- Child age > 36 months → Stage 5 stays active permanently
- Celebration dismissed accidentally → re-triggerable from Milestones

### Offline Edge Cases
- No internet → phrase cards display (text), audio shows unavailable
- Lose internet mid-Ask-Coach → "needs internet" message
- Audio fetch failure → text-only mode
- Concurrent audio play requests → previous stops, new plays

### Data Edge Cases
- Empty name in onboarding → validation error
- Future birthdate → validation error
- Birthdate = today → Stage 1, 0 months
- Clear app data → clean restart, server data recoverable via login
- Hive database corrupt → detect, wipe, re-sync from server

## Critical Paths (E2E)

1. **Full onboarding flow:** Install APK → open → PIPL consent → phone login → child info → Smart Home
2. **Core coaching flow:** Select scene → phrase card → play audio → tap reaction → mastery
3. **Adaptive coaching:** Tap "crying" → comfort phrases → "calm" → resume
4. **AI Coach text flow:** Tap Ask Coach → type Chinese → stream response → phrase card → feedback
5. **AI Coach voice flow [→E2E]:** Speak Chinese → ASR → LLM → TTS → phrase spoken aloud
6. **Celebration + share:** First "babbled" → celebration → share URL → WeChat landing page
7. **Couple mode flow:** Toggle → countdown → large text → done → return
8. **Sync flow:** Create events offline → go online → events synced → verify server
9. **Stage advancement:** Child ages → auto-advance → new scenes unlock → celebration
10. **Voice memo flow [→E2E]:** Sunday prompt → record 30s → auto-stop → upload to OSS → view in Journey
11. **Token lifecycle [→E2E]:** Login → use app → access token expires → auto-refresh → continue seamlessly
12. **Admin review flow:** Login admin → see pending → approve phrase → verify in knowledge base

## LLM Eval Tests [→EVAL]

- Ask Coach response must contain: English phrase + Chinese translation + coaching tip
- Ask Coach response must follow positive parenting methodology (no shaming, no punishment)
- Ask Coach response must be age-appropriate for the child's stage
- Ask Coach response for out-of-scope input (非育儿问题) → polite redirect
- Ask Coach response for edge scenario (baby ate something dangerous) → safety-first response
