# Copy to deploy/helm/babytalk-app/values-kind-secrets.yaml for local-only overrides.
# This example uses obviously fake dev values and must never be reused outside local kind.
secret:
  BABY_TALK_CONSUMER_JWT_SECRET: babytalk-consumer-dev-hs256-secret-not-for-prod
  BABY_TALK_AUTH_SENSITIVE_DATA_PEPPER: babytalk-auth-sensitive-data-pepper-not-for-prod
  BABY_TALK_ADMIN_JWT_SECRET: babytalk-admin-dev-hs256-secret-not-for-prod
  BABY_TALK_ADMIN_BOOTSTRAP_PASSWORD: SuperAdmin123!
