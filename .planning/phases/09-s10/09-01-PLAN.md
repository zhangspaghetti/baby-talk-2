---
phase: "09"
plan: "01"
type: "implementation"
---

<objective>
交付 incident-first mentor audit read model、admin API 与 contract proof
</objective>

<context>
Phase: 09 (s10) — Milestone: Admin 管理后台与后端多模块重构

先把“什么能被 truthful 地展示给 admin”锁死，再让浏览器去消费这个 contract。当前 mentor truth 已经稳定落在 `mentor_audit_logs` / `mentor_turns`，但没有 `conversation_id` bridge；所以这个任务必须把 S10 明确收口为 incident-first、read-only detail：基于 `correlation_id` 给 queue/detail、audit timeline、derived flag taxonomy 和 current live rate-limit status，绝不提前承诺 full transcript。
</context>

<must_haves>

- S10 detail 必须被文档和 API 一起定义成 incident-first evidence，而不是 full transcript；任何返回字段都不能暗示已持久化多轮 chat memory。
- `admin-api` 继续只依赖 `backend/common` read seam，不把 `app-api` runtime class 直接拉进编译依赖。
- `AdminMentorAuditWebTest` 必须覆盖 empty queue、missing delivered turn、403、401、404 和 live rate-limit snapshot，而不是只测 happy path。

</must_haves>
