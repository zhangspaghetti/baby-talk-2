---
phase: "09"
plan: "02"
type: "implementation"
---

<objective>
落地 mentor audit queue/detail UI、URL context return 与 end-to-end proof pack
</objective>

<context>
Phase: 09 (s10) — Milestone: Admin 管理后台与后端多模块重构

T01 会把 truthful backend seam 锁住；这个任务负责把它变成 admin 真能操作的浏览器路径。当前 `admin-web` 仍是登录 + protected stub proof，因此实现必须保持最小：先让浏览器真实保留 backend `permissions`，再在 minimal shell 中交付 mentor audit queue/detail、URL-based queue context return，以及一条可重放的 Playwright + root verify 路径；不要借机把完整 ProLayout / 设计清理 scope 拉回来。
</context>

<must_haves>

- 浏览器权限守卫必须消费 backend `permissions`；`mentor:audit` 不得继续靠猜测 `super_admin` role 或本地假数据放行。
- queue/detail 状态必须以 URL query params 为 truth source，关闭 detail 后仍能回到原过滤后的队列。
- Playwright seed 必须优先走真实 `/api/v1/mentor/chat` 路径，不能退回手写 DB fixture 来伪装 end-to-end 证明。

</must_haves>
