---
phase: "11"
plan: "03"
type: "implementation"
---

<objective>
把 S12 verifier 收成单入口 replay，并消除 compose 双启动漂移
</objective>

<context>
Phase: 11 (s12) — Milestone: Admin 管理后台与后端多模块重构

S13 / S14 / README / CONTRIBUTING 已经把 `tool/verify_m006_s12_control_plane_freshness.dart` 当成下钻入口。这个任务不能改路径，而要把它变成真正稳定的 single replay path：既证明 backend overview contract，也证明 browser control plane，并避免 verifier 与 Playwright global setup 各自再启动一次 compose。当前红边主要是 compose/browser boot，而不是缺一个新 feature。
</context>

<must_haves>

- `dart run tool/verify_m006_s12_control_plane_freshness.dart` 保持现有路径不变，并能一步重放 backend contract + browser control-plane proof。
- compose boot 不能同时由 verifier 和 Playwright global setup 各做一遍；失败时必须打印 step label 与 compose/runtime diagnostics。
- runbook 继续使用下游已引用的稳定路径，并指导 S13/S14/README 在 S12 失败时如何下钻。

</must_haves>
