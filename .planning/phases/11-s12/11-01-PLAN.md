---
phase: "11"
plan: "01"
type: "implementation"
---

<objective>
锁定四域 Overview backend contract、timeout degrade 语义与 MockMvc proof
</objective>

<context>
Phase: 11 (s12) — Milestone: Admin 管理后台与后端多模块重构

S12 是 closure slice，不是 greenfield。backend 已经有 Overview seam；这个任务要把它锁成 executor 可依赖的真相源。contract 必须严格限定为 `knowledge_ingestion`、`knowledge_kg`、`mentor_audit`、`distribution` 四个 domain，stream 继续只发 metadata。不要新增 users/admin-accounts summary，不要把 raw mentor/distribution payload 广播到 SSE，也不要把四个 domain read 包进一个事务里。
</context>

<must_haves>

- `GET /api/admin/overview/summary` 返回固定四个 domain，未授权 domain 保持 fail-closed，且 timeout 只降级对应 visible domain 而不是把整张 summary 一起打红。
- `GET /api/admin/overview/stream` 继续只传 `transport` / `heartbeat` metadata，并保持 `sinceEventId` 验证与 reconnect 计数可见；绝不透出 raw mentor/share payload。
- backend auth truth 与 S12 scope 对齐：没有任一 overview-domain 权限的管理员得到明确 403，而不是“空但成功”的 summary。

</must_haves>
