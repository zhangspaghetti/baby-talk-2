---
phase: "11"
plan: "02"
type: "implementation"
---

<objective>
对齐 `/overview` browser 权限、boss strip fallback 与 Playwright proof
</objective>

<context>
Phase: 11 (s12) — Milestone: Admin 管理后台与后端多模块重构

Overview 页面已经存在，但 browser route contract 还没有完全收口。这个任务要让 `/overview` 的前端访问边界、boss strip 状态机和 browser proof 都与 backend truth 一致：super_admin 和 overview-domain reader 能用，`users:read`-only 账号不能 deep-link 进来；realtime 断开时页面保留 last-good snapshot，truthfully 显示 `polling` / `recovered`，而不是假装一切 live。继续使用 `fetch` + SSE parser；在 Bearer-auth admin-api 上不要引入 native `EventSource` 第二条鉴权路径。
</context>

<must_haves>

- `/overview` route authorization 与 visible nav 必须和 admin-api overview 权限包络一致；`users:read`-only 管理员得到显式 403/redirect，而不是进入页面后只看到 summary error。
- boss strip 必须从 backend transport + client connectivity 共同导出 `live` / `polling` / `recovered`，并在 fallback/recovery 中持续保留 last-good snapshot。
- browser proof 必须覆盖 stale/fallback/malformed-contract 三类边界，并保持 hidden domains fail-closed。

</must_haves>
