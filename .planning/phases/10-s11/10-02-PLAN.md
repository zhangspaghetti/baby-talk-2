---
phase: "10"
plan: "02"
type: "implementation"
---

<objective>
交付 distribution stats workspace、受保护 landing resolver 与 browser proof
</objective>

<context>
Phase: 10 (s11) — Milestone: Admin 管理后台与后端多模块重构

在 backend contract 定稳后，再做最小 frontend/general-shell 改动，让 stats workspace 真能被用到。这个任务要沿用现有 plain `antd` + page-local fetch pattern，不把 S11 膨胀成 ProComponents/React Query/charting-library migration；重点是 URL-backed filters、visible workspace navigation、distribution-only landing correctness，以及用真实 public endpoints seed 出可见 stats。
</context>

<must_haves>

- `/distribution/stats` 必须把 `range` / `channel` 放进 URL query params，并在 reload/back 后保持一致；filters 失败时也不能被抹掉。
- shell 必须提供可见的 workspace landing / switcher，使只有 `distribution:read` 的管理员进入 stats，而有双权限的管理员仍能在 mentor audit 与 stats 间切换。
- UI 必须显式说明 channel 只作用于 release / share_card handoff，并继续保持 lightweight：不引入重 charting 依赖、React Query 或全面 ProComponents 迁移。

</must_haves>
