---
phase: "10"
plan: "01"
type: "implementation"
---

<objective>
锁定 truthful distribution stats read model、权限边界与 backend contract proof
</objective>

<context>
Phase: 10 (s11) — Milestone: Admin 管理后台与后端多模块重构

先把 truthful stats contract 锁死，再让 browser 去消费它。S11 的 backend 必须沿用现有 `release_distribution_events` / `share_landing_events` 真相源，但把查询边界明确限定成 `range=7d|30d|90d` + `channel=all|stable|beta` + 固定 detail limit，并把 response 拆成 `release*`、`share*` 与 normalized `detailRows[]`；share raw sections 永远不能假装自己有 channel。
</context>

<must_haves>

- stats contract 必须把 release distribution、share raw funnel 与 share→release handoff 分开表达；不能交付一个看起来统一但语义不真实的 merged funnel。
- `range` 只能接受 `7d|30d|90d`，`channel` 只能接受 `all|stable|beta`，并保持固定 recent-detail 上限；S11 不引入 rollup/backfill migration。
- payload 必须继续受 `distribution:read` 保护，并保持 redaction 边界：不输出 raw share token、consumer/session identifiers 或 secrets。

</must_haves>
