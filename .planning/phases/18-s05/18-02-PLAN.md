---
phase: "18"
plan: "02"
type: "implementation"
---

<objective>
把 `/users` placeholder 替换为 URL-backed ProTable、详情与禁用 UX
</objective>

<context>
Phase: 18 (s05) — Milestone: Admin 管理后台与后端多模块重构

在 T01 contract 到位后，把 S04 的 `Users` placeholder 换成真实 workbench：列表使用 ProTable，filters / pagination / selected account 用 URL query state 作为 truth source，detail surface 展示 account summary + sessions + consent audit，并通过必填 reason 的 destructive confirmation 驱动禁用动作。只抽出当前页面真正复用到的 queue/detail primitives，避免顺手重写其他模块页面。
</context>

<must_haves>

- `/users` 必须通过 ProTable 呈现 truthful accounts 数据，且 filters/pagination/selected account 在 URL 中可重放。
- 选中账号后能看到 session history 与 consent audit 证据，而不是只显示 status summary。
- disable 必须要求 reason、只对拥有 `users:write` 的管理员可见，并在成功后把当前 row/detail 刷到 deleted 状态。

</must_haves>
