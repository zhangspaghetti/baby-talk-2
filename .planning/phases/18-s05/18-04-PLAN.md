---
phase: "18"
plan: "04"
type: "implementation"
---

<objective>
补齐 compose-backed Playwright proof，验证用户禁用与管理员管理闭环
</objective>

<context>
Phase: 18 (s05) — Milestone: Admin 管理后台与后端多模块重构

最后用真实 browser proof 把 S05 的 stop condition 钉住：在现有 compose-backed Playwright harness 上新增 `users-management.spec.ts`，既覆盖 admin 登录后的 users table/detail/disable/admin-accounts flow，也通过真实 `app-api` mobile auth helper 证明“禁用用户后，旧 mobile access token 会因为 `account_deleted` 失败”。这里不允许 mocked API、localStorage-only fake 或 DB 直写捷径。
</context>

<must_haves>

- browser proof 必须跑在真实 compose stack 上，不引入 mocked API 或 localStorage-only fixture。
- spec 必须证明 disable 缺少 reason 不会成功，以及 disable 后旧 mobile token 对 `app-api` 返回稳定 `account_deleted`。
- spec 必须覆盖 super_admin 的 admin-account create/disable flow，以及 users-only 管理员对 `/users/admins` 的 hidden/forbidden 行为。

</must_haves>
