---
phase: "18"
plan: "03"
type: "implementation"
---

<objective>
挂上隐藏的 `/users/admins` 页面并复用现有 admin CRUD
</objective>

<context>
Phase: 18 (s05) — Milestone: Admin 管理后台与后端多模块重构

S05 还要把 super_admin 的管理员管理能力接到浏览器里，但不重复造后端。这个任务只扩展 S04 的 typed route catalog / permission typing，新增隐藏的 `/users/admins` route 与专用页面，复用 S03 已交付的 `/api/admin/admins` + `/api/admin/roles` 合同完成 create/disable admin flow，并从 Users workspace 给有权用户一个可达入口。
</context>

<must_haves>

- super_admin 能从 Users workspace 打开一个真实的“管理员管理”页面，而不是通过手工 URL 猜路径。
- 页面完全复用现有 admin RBAC/admin CRUD backend contract，不新增重复的 CRUD surface。
- create/disable actions 必须区分 `admins:read` 与 `admins:write`，并把 backend validation/forbidden 错误留在页面内可见。

</must_haves>
