---
phase: "18"
plan: "01"
type: "implementation"
---

<objective>
扩展 admin users 后端合同，补齐详情/历史/禁用语义
</objective>

<context>
Phase: 18 (s05) — Milestone: Admin 管理后台与后端多模块重构

先关闭 S05 的最高风险边界：在不让 `admin-api` 依赖 `app-api` service 的前提下，把现有只读 `/api/admin/users` 扩成真实 account-management contract。保持 shared-table truth，新增分页/过滤列表、单账号详情（summary + session history + consent audit）、以及带必填 reason 的禁用命令；任何禁用 side effect 都必须对齐 `AuthConsentSyncService.deleteAccount()` 已验证的 shared-table 行为，而不是重新发明第二套 lifecycle。
</context>

<must_haves>

- disable side effects 必须足够贴近既有 consumer delete semantics，使旧 mobile token 在后续 `app-api` 请求里稳定失败，而不是只改 admin 视图状态。
- users list/detail contract 必须 page/filter/detail ready，且 detail 只暴露 S05 需要的 account/session/consent evidence，不泄露 raw tokens。
- 测试同时证明 contract 与 shared-table 结果，而不是只证明 200 响应。

</must_haves>
