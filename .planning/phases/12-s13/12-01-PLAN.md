---
phase: "12"
plan: "01"
type: "implementation"
---

<objective>
把 S13 smoke 收口成真正的 live-stack-only proof，并持久化 front-door telemetry
</objective>

<context>
Phase: 12 (s13) — Milestone: Admin 管理后台与后端多模块重构

S13 research 已经证明 README / wrappers / verifier surface 基本都在，当前真正的 gap 不是“再做一套 onboarding”，而是让现有 smoke 行为说真话。`dev-verify-admin-demo` 现在宣称自己是 live-stack smoke，但仍然会重新进入 S12 的 heavyweight path（backend test、admin-web build、outer compose boot）。这个任务要保住 D108：无参 S12 verifier 仍是 canonical full replay，同时新增一个更薄的 live-stack-only mode，让 S13 smoke 只复用已健康的 runtime 做 runtime truth + browser proof。趁同一次手术，也把 demo/smoke 的 redaction-safe telemetry 统一收进共享 Dart verifier，避免 TTHW / 首个失败热点只活在一次性 stdout 里。
</context>

<must_haves>

- `dev-verify-admin-demo` 不再重跑 S12 的 `backend_contract`、`admin_web_build` 或 outer `compose_boot`，只依赖已健康的 live stack。
- demo/smoke 每次运行都要留下足够的 telemetry 字段，让 repo root 能推导出 TTHW、smoke pass rate 与 first-failure hotspot。
- focused root test 只读 tracked source files / inline fixtures，不依赖 gitignored history fixture 或 live Docker。

</must_haves>
