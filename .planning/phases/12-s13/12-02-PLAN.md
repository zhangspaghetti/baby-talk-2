---
phase: "12"
plan: "02"
type: "implementation"
---

<objective>
对齐 README / runbook / wrappers，并重放 POSIX + Windows front-door parity
</objective>

<context>
Phase: 12 (s13) — Milestone: Admin 管理后台与后端多模块重构

在 T01 让 smoke 说真话之后，这个任务只做 front-door closure：把 repo-root docs、runbook 与 `.sh/.cmd` wrappers 收口到同一故事，然后用真实 demo/smoke rerun 证明 fresh reader 看到的就是 runtime 真实行为。不要重写 release/Helm 文档，不要再发明第二条 command chain；只更新 S13 front door 真相与 drill-down 入口。
</context>

<must_haves>

- root docs 与 runbook 继续讲同一个 front-door 故事：先 `dev-up-admin-demo`，再 `dev-verify-admin-demo`，失败时下钻 S12 / S08 / S14，而不是重新发明命令链。
- `.sh` / `.cmd` wrappers 继续委托同一 verifier，并暴露同一套 `preflight` / `compose_boot` / `runtime_truth` / `live_stack_precondition` / `fast_smoke` vocabulary。
- fresh runtime proof 必须证明 demo ready、smoke 保留 live stack、并且 telemetry history 中存在 recent `demo` + `smoke` success entries。

</must_haves>
