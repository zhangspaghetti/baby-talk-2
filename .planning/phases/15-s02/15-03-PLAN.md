---
phase: "15"
plan: "03"
type: "implementation"
---

<objective>
升级 account snapshot 与 shared authenticated client，收口 token persistence/refresh
</objective>

<context>
Phase: 15 (s02) — Milestone: Admin 管理后台与后端多模块重构

先把 token state 本地化和 refresh/replay orchestration 收口到一个 seam，再让 household/mentor 去复用它。这个任务只动 account feature：扩展 `AccountSession` / `AccountLocalSnapshot` 保存 access/refresh token 与 legacy-compatible session metadata；新增 `AuthenticatedApiClient` 统一发 Authorization header、遇 401 只 refresh/replay 一次；`AccountRepository` 负责持久化 rotated tokens、terminal auth failure 时清 session 并暴露可见 phase。
</context>

<must_haves>

- 旧版 `account_state.json` 仍可读取；成功 refresh/sign-in 后会写回新 token schema，而不是强制 silent sign-out。
- 共享 auth client 对同一个失败请求最多 refresh/replay 一次；refresh terminal failure 时清 session、展示 redacted 错误、不泄露 raw token。

</must_haves>
