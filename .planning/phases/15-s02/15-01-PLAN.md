---
phase: "15"
plan: "01"
type: "implementation"
---

<objective>
定义 consumer JWT contract、V16 refresh schema 与 lifecycle proof
</objective>

<context>
Phase: 15 (s02) — Milestone: Admin 管理后台与后端多模块重构

先把 consumer JWT 的外部 contract 和持久化真相源定稳，再让 security/mobile 去消费它。这个任务只扩展 `AuthConsentSync*` 边界：SMS verify 继续创建现有 `account_sessions`，但现在同时签发 access/refresh token；refresh/logout 走新的 `account_refresh_tokens` rotation/revoke 表；旧 access token 必须像 admin half 一样在 refresh/logout 后立刻失效。
</context>

<must_haves>

- consumer refresh 表与 admin refresh 表继续隔离；`V16` 明确归 consumer JWT 使用，后续 RBAC migration 号顺延到 `V17+`。
- `/api/v1/auth/verify` / `/api/v1/auth/refresh` / `/api/v1/auth/logout` 对 rotated / expired / revoked / invalid token 给出稳定 code，并且 refresh/logout 后旧 access token 立即失效。

</must_haves>
