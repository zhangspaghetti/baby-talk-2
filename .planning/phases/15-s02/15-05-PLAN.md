---
phase: "15"
plan: "05"
type: "implementation"
---

<objective>
补 flyway-maven-plugin 并收口独立 db-migration CLI proof
</objective>

<context>
Phase: 15 (s02) — Milestone: Admin 管理后台与后端多模块重构

最后补 operational closure：`db-migration` 现在已经是唯一 Flyway owner，但 S02 还要兑现 module-local CLI path。这个任务只给 `backend/db-migration` 加 `flyway-maven-plugin` 与 fresh proof，不重新分散 Flyway owner，也不把 migrate 责任放回 `app-api` / `admin-api`。
</context>

<must_haves>

- `./backend/mvnw -f backend/db-migration/pom.xml flyway:migrate` 成为 slice-close proof 的一部分，而不是再起一次 `DbMigrationApplication` 充数。
- `db-migration` 仍是唯一 Flyway owner；V16 应被 smoke test 和 CLI migrate 一致识别。

</must_haves>
