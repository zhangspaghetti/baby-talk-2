---
phase: "15"
plan: "04"
type: "implementation"
---

<objective>
迁移 household/mentor Bearer consumers，并在 app wiring 上复用同一 auth client
</objective>

<context>
Phase: 15 (s02) — Milestone: Admin 管理后台与后端多模块重构

当 account feature 已经能安全持有/刷新 token 后，剩下的工作是把 household 和 mentor 两个 consumer 迁到同一个 Bearer canonical seam，而不是继续各自传 `sessionId`。这个任务负责更新 downstream service API、repository/viewmodel gating 和 app factory wiring，让三个 mobile 网络面复用同一 auth client / token store。
</context>

<must_haves>

- household / mentor 不再手工添加 `X-Session-Id`，而是复用 account feature 的 shared auth client；anonymous mentor chat path 继续存在。
- `mobile/lib/app/app.dart` 不再为 account/household/mentor 分别 new 三套裸 `http.Client()` auth seam；401/offline/consent banners 旧语义继续成立。

</must_haves>
