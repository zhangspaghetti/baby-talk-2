---
phase: "03"
plan: "03"
type: "implementation"
---

<objective>
四层 Prompt 构建 + search-mode 配置 + SpringAiMentorProvider 重构
</objective>

<context>
Phase: 03 (s03) — Milestone: MemPalace 育儿知识宫殿
</context>

<must_haves>

- MentorProperties 新增 searchMode 字段，application.yml 有对应配置
- MemPalacePromptBuilder 正确实现 L0/L1/L2-L3 分层
- SpringAiMentorProvider 在 agentic 模式下通过 .tools(palaceToolProvider) 注册工具
- searchMode=none 时行为与改造前完全一致（向后兼容）
- MentorProviderConfiguration 正确注入 PalaceToolProvider + PalaceSearchService
- 编译通过 + MemPalacePromptBuilderTest 通过 + 现有 SpringAiMentorProviderTest 仍通过

</must_haves>
