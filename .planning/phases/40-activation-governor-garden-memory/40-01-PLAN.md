---
phase: 40-activation-governor-garden-memory
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tool/verify_activation_governor_contract.dart
  - test/tool/verify_activation_governor_contract_test.dart
autonomous: true
requirements:
  - R063
  - R064
  - R065
requirements_addressed:
  - R063
  - R064
  - R065
user_setup: []
must_haves:
  truths:
    - "R063: Activation Governor is the only activation pacing authority; Pack/Graph, Runtime, and Garden authority bypasses fail."
    - "R064: Garden Memory meaningful states require parent confirmation and cannot be created from weak signals."
    - "R065: Explore/candidate generation can stay open while Activate intent is gated."
  artifacts:
    - path: "tool/verify_activation_governor_contract.dart"
      provides: "Independent Phase 40 contract verifier per D-01/D-02"
      exports:
        - "scanActivationGovernorContract"
        - "renderActivationGovernorContractReport"
        - "ActivationGovernorContractCase"
    - path: "test/tool/verify_activation_governor_contract_test.dart"
      provides: "RED/GREEN verifier tests for authority seams and structured cases"
  key_links:
    - from: "test/tool/verify_activation_governor_contract_test.dart"
      to: "tool/verify_activation_governor_contract.dart"
      via: "direct Dart import"
      pattern: "verify_activation_governor_contract"
    - from: "tool/verify_activation_governor_contract.dart"
      to: "mobile_v2/lib"
      via: "runtime scan root"
      pattern: "mobile_v2/lib"
---

<objective>
Create the independent Phase 40 Activation Governor / Garden Memory contract verifier as a TDD feature.

Purpose: Make R063/R064/R065 machine-checkable before any activation algorithm, Garden persistence, Runtime Agent payload, Strategy Pack schema, UI control, or metrics work exists.
Output: A pure Dart verifier with typed contract cases, structured report APIs, fail-closed boundary checks, CLI output, and root tests proving the authority seams.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
@C:/Users/zhang/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/40-activation-governor-garden-memory/40-SPEC.md
@.planning/phases/40-activation-governor-garden-memory/40-CONTEXT.md
@.planning/phases/40-activation-governor-garden-memory/40-RESEARCH.md
@.planning/phases/39-vnext-family-english-micro-ritual/39-SPEC.md
@.planning/phases/39-vnext-family-english-micro-ritual/39-VERIFICATION.md
@.planning/phases/39-vnext-family-english-micro-ritual/39-02-SUMMARY.md
@tool/verify_mobile_v2_semantic_firewall.dart
@test/tool/verify_mobile_v2_semantic_firewall_test.dart
@mobile_v2/lib/vnext_semantic_boundary.dart
</context>

<feature>
  <name>Independent Activation Governor contract verifier foundation</name>
  <files>tool/verify_activation_governor_contract.dart, test/tool/verify_activation_governor_contract_test.dart</files>
  <behavior>
    - Missing `mobile_v2/lib` produces a blocking missing-boundary violation.
    - Empty default contract cases produce a blocking contract-fixture violation.
    - Pack/Graph producer creating `active` fails unless Activation Governor is the decision source.
    - Runtime passes only when consuming an existing Governor decision; Runtime self-governance fails.
    - Garden passes when presenting state or collecting parent confirmation; Garden-owned activation policy fails.
    - `candidate -> active` requires parent intent/readiness plus Governor `allow_activation`.
    - Positive Explore/candidate examples pass when they avoid action-now activation intent.
  </behavior>
  <implementation>
    Implement a pure Dart verifier patterned after `tool/verify_mobile_v2_semantic_firewall.dart`: exported typed case/report/violation objects, deterministic sorting, renderer, CLI usage parsing, success marker, no package installs, and no JSON/YAML fixtures.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - encode independent verifier and authority-seam expectations</name>
  <files>test/tool/verify_activation_governor_contract_test.dart</files>
  <behavior>
    - Test fail-closed missing `mobile_v2/lib`.
    - Test missing/default-empty contract cases fail closed.
    - Test positive Explore candidate cases pass without Governor when they do not imply family action now.
    - Test negative Pack/Graph direct activation, Runtime self-governance, and Garden-owned activation policy.
    - Test positive Runtime consuming an existing Governor `allow_activation` decision.
    - Test `candidate -> active` requires both parent intent/readiness and Governor decision.
  </behavior>
  <action>Create failing tests first for the public API names `scanActivationGovernorContract`, `renderActivationGovernorContractReport`, `ActivationGovernorContractCase`, and CLI option parsing. Cite D-01/D-02 by requiring a new verifier file instead of extending `verify_mobile_v2_semantic_firewall.dart`; cite D-03/D-04 by scoping temp-project scans to `mobile_v2/lib` and typed contract cases only; cite D-05/D-06/D-16/D-17 by making typed Dart cases the authority backbone and rejecting JSON/YAML fixture files; cite D-10/D-11/D-12/D-13/D-14/D-15 by requiring all three authority seams to fail closed. Do not create a project-local skill per D-09.</action>
  <verify>
    <automated>Run `./flutter.cmd test test\tool\verify_activation_governor_contract_test.dart` from repo root and confirm the RED tests fail because `tool/verify_activation_governor_contract.dart` and verifier behavior do not exist yet. If `./flutter.cmd` stalls or SDK/user-profile writes block, use `C:\software\flutter\bin\cache\dart-sdk\bin\dart.exe --packages=C:\software\flutter\packages\flutter_tools\.dart_tool\package_config.json C:\software\flutter\bin\cache\flutter_tools.snapshot test test\tool\verify_activation_governor_contract_test.dart`.</automated>
  </verify>
  <done>RED tests exist, fail for missing implementation or missing behavior, and cover R063/R064/R065 authority expectations without introducing algorithm, schema, API, UI, Runtime payload, Pack schema, Primitive sequencing, or metrics decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - implement structured verifier, report, and CLI</name>
  <files>tool/verify_activation_governor_contract.dart, test/tool/verify_activation_governor_contract_test.dart</files>
  <behavior>
    - Tests from Task 1 pass.
    - CLI prints a stable status/report and exits nonzero for blocking violations.
    - Unknown CLI args return usage error without scanning.
    - Direct SDK command can run `tool\verify_activation_governor_contract.dart` without `dart run`.
  </behavior>
  <action>Implement the verifier as a separate repo-owned Dart artifact per D-01 and D-02. Provide `ActivationGovernorContractCase` fields named in D-17, including `id`, `description`, `surface`, `producer`, `consumer`, `decisionSource`, `text`, `gardenAction`, `hasGovernorDecision`, `hasParentIntent`, `requiresGovernorDecision`, `requiresParentConfirmation`, `weakSignalOnly`, `expectedPass`, and `expectedReason`. Add default typed cases covering D-18 baseline groups for positive Explore, positive Runtime-consuming-existing-decision, negative Pack/Graph activation shortcut, negative Runtime self-governance, and negative Garden-owned policy. Enforce R063 by allowing only Governor-authored pacing decisions and rejecting Pack/Graph/Runtime/Garden bypasses; enforce R065 by allowing candidate/explore content without activation intent. Keep all rule names contract-level and do not lock policy algorithms, thresholds beyond the contract-level capacity meaning, persistence, API shape, UI controls, Runtime Agent payload schema, Strategy Pack schema, Primitive sequencing, or metrics instrumentation.</action>
  <verify>
    <automated>Run `./flutter.cmd test test\tool\verify_activation_governor_contract_test.dart`; fallback if needed to `C:\software\flutter\bin\cache\dart-sdk\bin\dart.exe --packages=C:\software\flutter\packages\flutter_tools\.dart_tool\package_config.json C:\software\flutter\bin\cache\flutter_tools.snapshot test test\tool\verify_activation_governor_contract_test.dart`.</automated>
    <automated>Run `C:\software\flutter\bin\cache\dart-sdk\bin\dart.exe tool\verify_activation_governor_contract.dart --help` and `C:\software\flutter\bin\cache\dart-sdk\bin\dart.exe tool\verify_activation_governor_contract.dart` from repo root.</automated>
  </verify>
  <done>The new verifier reports pass/fail deterministically, fails closed for missing boundary or invalid default cases, passes the authority-seam tests, and prints a Phase 40 success marker only when there are no blocking violations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR - stabilize public verifier contract and guard against accidental scope expansion</name>
  <files>tool/verify_activation_governor_contract.dart, test/tool/verify_activation_governor_contract_test.dart</files>
  <behavior>
    - Public API remains readable and table-driven after cleanup.
    - Report rendering remains stable for checker/proof use.
    - Tests assert no JSON/YAML fixture path and no repo-wide scan behavior exists in this phase.
  </behavior>
  <action>Refactor only after tests pass. Extract small private helpers for normalized strings, decision-source checks, and violation sorting if they make the verifier easier to inspect. Add explicit tests or source assertions that the verifier does not scan repo-wide per D-04 and does not introduce JSON/YAML fixtures per D-16. Preserve the D-31/D-32/D-33 matrix as contract language in comments or report labels only if useful, but do not turn it into an API payload, database field list, class-name lock, event-name lock, UI-control lock, or Runtime Agent payload schema.</action>
  <verify>
    <automated>Run `./flutter.cmd test test\tool\verify_activation_governor_contract_test.dart`; fallback if needed to the direct `flutter_tools.snapshot test` command from Task 2.</automated>
    <automated>Run `C:\software\flutter\bin\cache\dart-sdk\bin\dart.exe tool\verify_activation_governor_contract.dart`.</automated>
  </verify>
  <done>Verifier code is organized around stable public contracts, all focused tests pass, and no out-of-scope implementation surface is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract fixture -> verifier | Typed fixture data is untrusted proof input and must fail closed when it implies activation bypass or Garden truth without confirmation. |
| `mobile_v2/lib` -> verifier | vNext runtime files are scanned as active product truth, unlike docs/reference/quarantine material. |
| Future Pack/Graph/Runtime/Garden -> contract | Future implementation must be constrained by this authority contract before it can activate family routine states. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Elevation of Privilege | Pack/Graph, Runtime, Garden authority seams | mitigate | Structured cases fail Pack/Graph activation, Runtime self-governance, and Garden-owned activation policy per D-10 through D-15. |
| T-40-02 | Tampering | `candidate -> active` transition | mitigate | Verifier requires parent intent/readiness plus Governor `allow_activation` before `active` can pass. |
| T-40-03 | Repudiation | Contract proof | mitigate | Renderer emits stable violation reasons and counts so failures are attributable to specific fixture IDs or source lines. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install is planned; pure Dart/stdlib implementation keeps the package legitimacy audit empty. |
</threat_model>

<verification>
Plan-level gate:
- `./flutter.cmd test test\tool\verify_activation_governor_contract_test.dart`
- Fallback: `C:\software\flutter\bin\cache\dart-sdk\bin\dart.exe --packages=C:\software\flutter\packages\flutter_tools\.dart_tool\package_config.json C:\software\flutter\bin\cache\flutter_tools.snapshot test test\tool\verify_activation_governor_contract_test.dart`
- `C:\software\flutter\bin\cache\dart-sdk\bin\dart.exe tool\verify_activation_governor_contract.dart`
</verification>

<success_criteria>
- RED and GREEN TDD gate commits exist for `40-01` or the SUMMARY explicitly records any TDD gate violation.
- `tool/verify_activation_governor_contract.dart` is independent from the Phase 39 verifier while reusing its pure Dart report/CLI pattern.
- R063/R064/R065 authority expectations are represented in typed contract cases.
- D-01 through D-17 are implemented or explicitly constrained in this verifier foundation, with only the baseline D-18 authority-seam groups assigned here: positive Explore, positive Runtime-consuming-existing-decision, negative Pack/Graph activation shortcut, negative Runtime self-governance, and negative Garden-owned policy. Full D-18 fixture coverage remains assigned to Plan 40-02.
- No activation algorithm, database schema, API payload, UI control, Runtime Agent payload schema, Strategy Pack schema, Primitive sequencing, or metrics instrumentation is created.
</success_criteria>

<output>
Create `.planning/phases/40-activation-governor-garden-memory/40-01-SUMMARY.md` when done.
</output>
