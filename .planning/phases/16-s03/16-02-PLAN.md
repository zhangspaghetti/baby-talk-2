---
phase: "16"
plan: "02"
type: "implementation"
---

<objective>
改成 DB-backed authorities，并让 auth contract 返回 current permissions
</objective>

<context>
Phase: 16 (s03) — Milestone: Admin 管理后台与后端多模块重构

R051 最大风险不是“有没有 role 表”，而是权限变更后 JWT 还能不能继续偷跑。这个任务把 admin-api 的 effective authorities 改成按 `principalId` 从数据库实时读取，同时保持 S02 已验证的 login/refresh/logout/session-guard 生命周期不变，并把 current permissions 暴露给 `/api/admin/me` 与 login/refresh payload。
</context>

<must_haves>

- admin effective authorities 的 truth source 是 DB snapshot，不是 JWT 里签发时刻的 `roles` claim。
- login / refresh / `/api/admin/me` 的 admin payload 对齐成 current `roles` + `permissions` contract，并保持现有 401/400 错误包体稳定。

</must_haves>
