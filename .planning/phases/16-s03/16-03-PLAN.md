---
phase: "16"
plan: "03"
type: "implementation"
---

<objective>
交付 admin/users/roles/admins API 与 MockMvc 权限矩阵 proof
</objective>

<context>
Phase: 16 (s03) — Milestone: Admin 管理后台与后端多模块重构

在 shared data seam 与 DB-backed authorities 就绪后，这个任务交付 S03 真正对外可见的 operator contract：super_admin 管理 roles/admin principals，limited admin 只能读 users。这里只做 backend API 与 proof，不拉入 admin-web、browser guard 或 runbook。
</context>

<must_haves>

- super_admin 可创建并禁用 admin principal；limited admin 仅凭 `users:read` 能读 users API，不能写 admin/RBAC。
- `AdminRbacWebTest` 必须是 SpringBootTest + MockMvc + Postgres fixture proof，覆盖 200/403/401 与 same-token immediate-effect 场景，而不是 mock-based unit tests。

</must_haves>
