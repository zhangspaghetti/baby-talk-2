---
phase: "16"
plan: "01"
type: "implementation"
---

<objective>
落地 V17 权限 schema、shared permission catalog 与 JDBC data seam
</objective>

<context>
Phase: 16 (s03) — Milestone: Admin 管理后台与后端多模块重构

先把 migration 编号、shared permission vocabulary 和 JDBC data seam 定稳，再让 security/controllers 在其上闭环。这个任务只落地 S03 的基础设施：`V17` RBAC permissions schema、`backend/common` shared repos/catalog、以及 `admin-api` 显式 wiring；不在这里加 controller surface，也不引入 `app-api` 编译依赖。
</context>

<must_haves>

- `V17` 成为唯一 RBAC migration slot，permission vocabulary 用代码+SQL 同时固定，后续 S05/S06/S07 直接复用。
- shared repo seam 留在 `backend/common` 且保持 plain class + explicit bean wiring；`admin-api` 不增加对 `app-api` 的编译依赖。

</must_haves>
