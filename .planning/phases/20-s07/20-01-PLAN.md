---
phase: "20"
plan: "01"
type: "implementation"
---

<objective>
Assemble the combined mentor + distribution closure proof pack
</objective>

<context>
Phase: 20 (s07) — Milestone: Admin 管理后台与后端多模块重构

S10 and S11 already shipped the runtime seams; the open risk is that nothing currently proves they still compose together through the shared shell. Keep this task surgical: add one slice-level browser proof plus one repo-root verifier, reuse the current focused tests/helpers, and do not reopen feature scope just because S07 never had its own plan artifact.
</context>

<must_haves>

- The new closure proof must fail if either workspace disappears from the shared shell or if route/auth truth drifts away from live `/api/admin/me` permissions.
- The closure proof must explicitly cover mentor incident-first semantics and distribution release-only channel/redaction semantics together, not as two disconnected screenshots.
- `tool/verify_m006_s07_mentor_distribution.dart` must be runnable from repo root and exit non-zero on any backend/build/browser failure.

</must_haves>
