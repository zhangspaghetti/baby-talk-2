---
phase: "08"
plan: "02"
type: "implementation"
---

<objective>
Replace the Overview placeholder with visible freshness + fallback proof
</objective>

<context>
Phase: 08 (s09) — Milestone: Admin 管理后台与后端多模块重构
</context>

<must_haves>

- `Overview` renders real multi-domain freshness content instead of placeholder copy.
- Operators can see when they are on polling fallback and when realtime has recovered, while the last good snapshot stays visible.
- The S12 proof pack exercises `summary -> SSE loss -> polling fallback -> recovery` with fail-closed auth and no fake transcript/share-token data.

</must_haves>
