---
phase: "08"
plan: "03"
type: "implementation"
---

<objective>
Create the repo-root admin demo + fast-smoke front door
</objective>

<context>
Phase: 08 (s09) — Milestone: Admin 管理后台与后端多模块重构
</context>

<must_haves>

- The new repo-root scripts become the primary admin demo/smoke entrypoints; stale `run-mobile-e2e.*` and `verify-e2e.sh` stay as historical references rather than the README front door.
- Failures name the failing stage, likely cause, and next action on both Windows and POSIX.
- The front-door docs are truthful for the split stack and no longer contain the default Flutter README template or the old “no Maven wrapper” lie.

</must_haves>
