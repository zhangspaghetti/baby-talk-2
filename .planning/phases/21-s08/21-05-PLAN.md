---
phase: "21"
plan: "05"
type: "implementation"
---

<objective>
Align deploy-truth documentation to the split-stack release path
</objective>

<context>
Phase: 21 (s08) — Milestone: Admin 管理后台与后端多模块重构
</context>

<must_haves>

- runbook 不再提单个 `babytalk/backend` workload 作为 M006 的真实部署模型。
- runbook 明确记录 `db-migration` 先于 app/admin 服务的约束，以及 `admin-api` internal-only 的边界。
- 失败排查入口要指向新的 verifier / k8s smoke / Helm template，而不是过时脚本。

</must_haves>
