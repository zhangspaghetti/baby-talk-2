---
phase: "21"
plan: "02"
type: "implementation"
---

<objective>
Wire GitHub Actions to replay the runtime gate with a Docker relay and Playwright artifacts
</objective>

<context>
Phase: 21 (s08) — Milestone: Admin 管理后台与后端多模块重构
</context>

<must_haves>

- CI job 复用 repo-root verifier，而不是在 workflow YAML 里重新拼装一套 release 顺序。
- `localhost:2375` 兼容性在 workflow 内被显式解决，三个 backend 模块 POM 保持不变。
- Playwright report / trace artifact 在 browser 失败时仍然能拿到。

</must_haves>
