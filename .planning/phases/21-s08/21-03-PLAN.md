---
phase: "21"
plan: "03"
type: "implementation"
---

<objective>
Rewrite the Helm chart around explicit app/admin/admin-web workloads
</objective>

<context>
Phase: 21 (s08) — Milestone: Admin 管理后台与后端多模块重构
</context>

<must_haves>

- chart default + production values 都会渲染 `app-api`、`admin-api`、`admin-web` 和 migration job/hook。
- `admin-api` 不出现在 public ingress；`admin-web` 通过 chart-controlled upstream 代理到 internal admin-api。
- 旧的单 workload `babytalk/backend` 假设不再是 chart 的默认输出。

</must_haves>
