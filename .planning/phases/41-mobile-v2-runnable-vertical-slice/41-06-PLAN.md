---
phase: 41-mobile-v2-runnable-vertical-slice
plan: "06"
type: tdd
wave: 1
depends_on: ["41-01"]
files_modified:
  - mobile_v2/lib/features/ritual_room/data/dto/interaction_input_dto.dart
  - mobile_v2/lib/features/ritual_room/data/dto/interaction_advance_request.dart
  - mobile_v2/lib/features/ritual_room/data/dto/interaction_snapshot_response.dart
  - mobile_v2/lib/features/ritual_room/data/dto/interaction_result_response.dart
  - mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart
  - mobile_v2/test/features/ritual_room/data/mappers/interaction_mapper_test.dart
autonomous: true
requirements: [R060, R067]
user_setup: []
must_haves:
  truths:
    - "All five InputEvent variants map losslessly to stable wire types while expectedRevision remains request-level."
    - "Schema version 1 accepts unknown optional fields but rejects missing required fields; unsupported schema maps explicitly."
    - "Transport responses expose ProductSnapshot/result only and never ConsistencyState, ReplayJournal, TransitionRecord, or fingerprints."
  artifacts:
    - path: mobile_v2/lib/features/ritual_room/data/dto/interaction_input_dto.dart
      provides: "Typed five-variant transport input"
    - path: mobile_v2/lib/features/ritual_room/data/dto/interaction_result_response.dart
      provides: "Status/error plus snapshot/latestSnapshot transport result"
    - path: mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart
      provides: "Strict exhaustive domain/transport mapping"
  key_links:
    - from: mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart
      to: mobile_v2/lib/features/ritual_room/domain/models/input_event.dart
      via: "exhaustive five-channel mapping"
      pattern: "inputToDomain"
    - from: mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart
      to: mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart
      via: "schema-checked snapshot mapping"
      pattern: "currentSchemaVersion"
---

<objective>
TDD strict interaction transport DTOs and the exhaustive boundary mapper independently from API/repository delegation.

Purpose: Freeze the stable API-shaped contract while preventing transport from defining domain semantics or exposing engine-internal truth/evidence.
Output: Four immutable DTOs, one mapper, and focused compatibility/privacy tests.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
@mobile_v2/AGENTS.md
@mobile_v2/CODING_STANDARDS.md
@docs/superpowers/plans/2026-06-19-interaction-engine-flutter-riverpod.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Specify five-channel wire and schema/privacy contracts</name>
  <files>mobile_v2/test/features/ritual_room/data/mappers/interaction_mapper_test.dart</files>
  <read_first>
    @mobile_v2/lib/features/ritual_room/domain/models/input_event.dart
    @mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart
    @mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
    @docs/superpowers/plans/2026-06-19-interaction-engine-flutter-riverpod.md
  </read_first>
  <behavior>
    - reaction_selection, voice_observation, free_text, future_signal, and strategy_preference round-trip with eventId/timestamp/payload intact.
    - expectedRevision exists on InteractionAdvanceRequest, never inside input payload.
    - Strategy preference remains an input event and never becomes StrategyDecision in mapping.
    - Schema 1 ignores unknown optional fields; missing required fields fail; schema other than 1 is unsupported.
    - Result JSON contains status/error and snapshot/latestSnapshot only, excluding all consistency/replay/fingerprint fields.
  </behavior>
  <action>Write the mapper contract first with explicit R060 observation/hypothesis safety and R067 five-channel completeness. Include every advance result code, malformed payloads, unknown optional fields, unsupported schema, and ProductSnapshot-only serialization. Capture RED and require missing `InteractionInputDto`, `InteractionAdvanceRequest`, or `InteractionMapper` evidence.</action>
  <acceptance_criteria>
    - Test assertion: five wire type strings are enumerated and round-tripped.
    - Test assertion: expectedRevision placement and strategy-preference separation are explicit.
    - Test assertion: schema-1 compatibility and unsupported-schema behavior are distinct.
    - Privacy assertion: serialized result is checked for absence of processedEvents, inputFingerprint, ConsistencyState, ReplayJournal, and TransitionRecord.
    - RED assertion: captured output identifies a missing DTO/mapper production symbol.
  </acceptance_criteria>
  <verify>
    <automated>$out=Join-Path $env:TEMP '41-06-wire-red.out'; $err=Join-Path $env:TEMP '41-06-wire-red.err'; Push-Location mobile_v2; try { $p=Start-Process -FilePath flutter -ArgumentList @('test','test/features/ritual_room/data/mappers/interaction_mapper_test.dart') -Wait -PassThru -NoNewWindow -RedirectStandardOutput $out -RedirectStandardError $err } finally { Pop-Location }; $text=(Get-Content -Raw $out)+(Get-Content -Raw $err); if($p.ExitCode -eq 0){ throw 'expected RED but mapper test passed' }; if($text -notmatch 'InteractionInputDto|InteractionAdvanceRequest|InteractionMapper'){ throw 'RED output did not identify a missing wire symbol' }</automated>
  </verify>
  <done>The expected wire-contract RED state is mechanically proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN/REFACTOR: Implement strict DTOs and exhaustive mapper</name>
  <files>mobile_v2/lib/features/ritual_room/data/dto/interaction_input_dto.dart, mobile_v2/lib/features/ritual_room/data/dto/interaction_advance_request.dart, mobile_v2/lib/features/ritual_room/data/dto/interaction_snapshot_response.dart, mobile_v2/lib/features/ritual_room/data/dto/interaction_result_response.dart, mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart</files>
  <read_first>
    @mobile_v2/test/features/ritual_room/data/mappers/interaction_mapper_test.dart
    @mobile_v2/lib/features/ritual_room/domain/models/input_event.dart
    @mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart
    @mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart
    @mobile_v2/CODING_STANDARDS.md
  </read_first>
  <action>Implement immutable manual DTO parsing and exhaustive mapper switches. Validate required fields and typed payload members without `dynamic` fallbacks. Preserve raw voice/free text only inside the transient request DTO. Check `ProductSnapshot.currentSchemaVersion` before interpretation. Ignore unknown optional schema-1 fields without storing them. Map every result status/error explicitly, carrying latestSnapshot for revision/event-ID conflicts. Never expose consistency/replay/fingerprint state, choose policy, generate utterances, initialize sessions, or add code generation.</action>
  <acceptance_criteria>
    - Behavior assertion: mapper test passes and DTO/mapper analyzer exits 0.
    - Source assertion: mapper contains exhaustive handling for all five inputs and all result codes.
    - Source assertion: `expectedRevision` occurs in request DTO and not input DTO payload construction.
    - Source assertion: response DTO files contain none of `ConsistencyState`, `ReplayJournal`, `TransitionRecord`, or `inputFingerprint`.
    - Requirement assertion: R060 and R067 mapping behaviors pass without engine-policy leakage.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location mobile_v2; try { flutter test test/features/ritual_room/data/mappers/interaction_mapper_test.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; dart analyze lib/features/ritual_room/data/dto lib/features/ritual_room/data/mappers; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>$files=Get-ChildItem mobile_v2/lib/features/ritual_room/data/dto -Filter *.dart; $hits=$files | Select-String -Pattern 'ConsistencyState|ReplayJournal|TransitionRecord|inputFingerprint'; if($hits){ $hits | Out-String | Write-Error; throw 'transport exposes engine-internal state' }</automated>
  </verify>
  <done>Five-channel schema-aware mapping passes, and transport exposes only product snapshots/results.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transport payload -> DTO | Malformed/untrusted external-shaped values enter parsing. |
| DTO -> domain | Wire names must not redefine product semantics. |
| engine result -> response | Private correctness/evidence state must not leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-06-01 | Tampering | DTO parsing | mitigate | Required-field/type tests and explicit unsupported schema. |
| T-41-06-02 | Information Disclosure | response DTO | mitigate | ProductSnapshot-only serialization tests and source scan. |
| T-41-06-03 | Elevation of Privilege | mapper | mitigate | Exhaustive conversion only; no policy/lifecycle APIs. |
</threat_model>

<verification>
Run expected-RED proof, focused mapper tests, analyzer, and engine-internal response-field scan.
</verification>

<success_criteria>
- Every engine input has a stable wire representation.
- Schema and revision remain independent.
- DTOs are transport contracts, not domain definitions.
- Private runtime correctness/evidence never crosses transport.
</success_criteria>

<output>
Create `.planning/phases/41-mobile-v2-runnable-vertical-slice/41-06-SUMMARY.md` when done.
</output>
