---
phase: 41-mobile-v2-runnable-vertical-slice
plan: "07"
type: tdd
wave: 3
depends_on: ["41-04", "41-06"]
files_modified:
  - mobile_v2/lib/features/ritual_room/domain/repositories/interaction_repository.dart
  - mobile_v2/lib/features/ritual_room/data/datasources/interaction_api.dart
  - mobile_v2/lib/features/ritual_room/data/datasources/mock_interaction_api.dart
  - mobile_v2/lib/features/ritual_room/data/repositories/interaction_repository_impl.dart
  - mobile_v2/test/fixtures/interaction_test_fixtures.dart
  - mobile_v2/test/helpers/interaction_test_doubles.dart
  - mobile_v2/test/features/ritual_room/data/datasources/mock_interaction_api_test.dart
  - mobile_v2/test/features/ritual_room/data/repositories/interaction_repository_test.dart
autonomous: true
requirements: [R060, R067]
user_setup: []
must_haves:
  truths:
    - "MockInteractionApi delegates getSnapshot/advance to InteractionEnginePort exactly once and cannot initialize sessions."
    - "InteractionRepository maps domain input to request DTO and response DTO to domain result without caching or mutating ProductSnapshot."
    - "Direct engine and adapter execution produce equal results for all five channels and every conflict/error status."
  artifacts:
    - path: mobile_v2/lib/features/ritual_room/data/datasources/mock_interaction_api.dart
      provides: "Thin API-shaped adapter over InteractionEnginePort"
    - path: mobile_v2/lib/features/ritual_room/domain/repositories/interaction_repository.dart
      provides: "Presentation-facing domain repository contract"
    - path: mobile_v2/lib/features/ritual_room/data/repositories/interaction_repository_impl.dart
      provides: "Conversion-only transport implementation"
  key_links:
    - from: mobile_v2/lib/features/ritual_room/data/datasources/mock_interaction_api.dart
      to: mobile_v2/lib/features/ritual_room/domain/engine/interaction_engine_port.dart
      via: "one getSnapshot/advance delegation"
      pattern: "InteractionEnginePort"
    - from: mobile_v2/lib/features/ritual_room/data/repositories/interaction_repository_impl.dart
      to: mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart
      via: "domain-request-response-domain conversion"
      pattern: "InteractionMapper"
---

<objective>
TDD the thin interaction API and repository adapters over InteractionEnginePort.

Purpose: Prove local mock transport and a future remote transport can change without moving lifecycle, revision, idempotency, normalization, strategy, utterance, replay, or product-truth authority.
Output: Domain repository contract, API/mock API, repository implementation, shared test fixtures/doubles, and parity/delegation tests.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
@mobile_v2/AGENTS.md
@mobile_v2/CODING_STANDARDS.md
@docs/superpowers/plans/2026-06-19-interaction-engine-flutter-riverpod.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-04-SUMMARY.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Specify thin delegation, result parity, and repository conversion</name>
  <files>mobile_v2/test/fixtures/interaction_test_fixtures.dart, mobile_v2/test/helpers/interaction_test_doubles.dart, mobile_v2/test/features/ritual_room/data/datasources/mock_interaction_api_test.dart, mobile_v2/test/features/ritual_room/data/repositories/interaction_repository_test.dart</files>
  <read_first>
    @mobile_v2/lib/features/ritual_room/domain/engine/interaction_engine_port.dart
    @mobile_v2/lib/features/ritual_room/domain/engine/interaction_engine.dart
    @mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart
    @mobile_v2/lib/features/ritual_room/data/dto/interaction_result_response.dart
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
  </read_first>
  <behavior>
    - Mock API snapshot read delegates once and never invokes initialize.
    - Mock API advance delegates once with unchanged interactionId/expectedRevision/domain input.
    - Repository returns domain ProductSnapshot/AdvanceResult only for applied, duplicate, every conflict, not-found, invalid, unsupported-schema, and pipeline-failed paths.
    - All five channels produce adapter results equal to direct engine execution.
    - Adapters retain no mutable snapshot, raw request, retry ID, or policy table.
  </behavior>
  <action>Create focused immutable fixtures and fakes, including a fake `InteractionEnginePort` with call counts and no initializer capability. Write API/repository tests before production adapters. Explicitly cover R060 safe context transport and R067 five-channel/parity behavior. Capture RED and require missing `InteractionApi`, `MockInteractionApi`, or `InteractionRepositoryImpl` evidence.</action>
  <acceptance_criteria>
    - Test assertion: getSnapshot and advance delegation counts are exactly one and initialize count is impossible/absent.
    - Test assertion: every result code and latestSnapshot propagation is covered.
    - Test assertion: direct-engine and adapter ProductSnapshot equality covers all product-semantic fields.
    - Test assertion: five event types pass through the same repository method.
    - RED assertion: captured output identifies a missing API/repository production symbol.
  </acceptance_criteria>
  <verify>
    <automated>$out=Join-Path $env:TEMP '41-07-adapter-red.out'; $err=Join-Path $env:TEMP '41-07-adapter-red.err'; Push-Location mobile_v2; try { $p=Start-Process -FilePath flutter -ArgumentList @('test','test/features/ritual_room/data/datasources/mock_interaction_api_test.dart','test/features/ritual_room/data/repositories/interaction_repository_test.dart') -Wait -PassThru -NoNewWindow -RedirectStandardOutput $out -RedirectStandardError $err } finally { Pop-Location }; $text=(Get-Content -Raw $out)+(Get-Content -Raw $err); if($p.ExitCode -eq 0){ throw 'expected RED but adapter tests passed' }; if($text -notmatch 'InteractionApi|MockInteractionApi|InteractionRepositoryImpl'){ throw 'RED output did not identify a missing adapter symbol' }</automated>
  </verify>
  <done>The expected adapter RED state is proven against absent production boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN/REFACTOR: Implement conversion-only API and repository boundaries</name>
  <files>mobile_v2/lib/features/ritual_room/domain/repositories/interaction_repository.dart, mobile_v2/lib/features/ritual_room/data/datasources/interaction_api.dart, mobile_v2/lib/features/ritual_room/data/datasources/mock_interaction_api.dart, mobile_v2/lib/features/ritual_room/data/repositories/interaction_repository_impl.dart</files>
  <read_first>
    @mobile_v2/test/features/ritual_room/data/datasources/mock_interaction_api_test.dart
    @mobile_v2/test/features/ritual_room/data/repositories/interaction_repository_test.dart
    @mobile_v2/test/helpers/interaction_test_doubles.dart
    @mobile_v2/lib/features/ritual_room/data/mappers/interaction_mapper.dart
    @docs/superpowers/plans/2026-06-19-interaction-engine-flutter-riverpod.md
  </read_first>
  <action>Implement `InteractionApi.getSnapshot/advance`, a `MockInteractionApi` that receives `InteractionEnginePort` plus mapper, and `InteractionRepository`/implementation. The API maps DTO request to domain call and domain result to DTO. The repository maps domain input to request DTO and response DTO to domain result. Do not create a second/mock engine, expose initialize, cache/mutate snapshots, normalize, accumulate, choose strategy, generate utterances, increment revisions, append replay, inspect CapabilityMask, retry with a new event ID, or retain raw input.</action>
  <acceptance_criteria>
    - Behavior assertion: focused API/repository tests and full data tests pass.
    - Source assertion: MockInteractionApi depends on InteractionEnginePort, not concrete InteractionEngine or initializer.
    - Source assertion: adapter/repository files contain no business-rule literals or four module implementation references.
    - Behavior assertion: adapter parity with direct engine passes for every channel and result path.
    - Gate assertion: both Phase 39/40 verifier CLIs exit 0.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location mobile_v2; try { flutter test test/features/ritual_room/data; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; dart run ../tool/verify_mobile_v2_semantic_firewall.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; dart run ../tool/verify_activation_governor_contract.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>$files=@('mobile_v2/lib/features/ritual_room/data/datasources/mock_interaction_api.dart','mobile_v2/lib/features/ritual_room/data/repositories/interaction_repository_impl.dart'); $hits=Select-String -Path $files -Pattern 'signalAggregation|pressureLevel|reduce_options|low_joinability|revision\s*\+|NormalizeEngine|StateAccumulator|StrategyEngine|UtteranceEngine|InteractionSessionInitializer'; if($hits){ $hits | Out-String | Write-Error; throw 'adapter contains authority or policy logic' }</automated>
  </verify>
  <done>Interaction API/repository adapters are thin, parity-tested, privacy-preserving, and authority-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DTO request -> InteractionEnginePort | Transport input enters the sole authority. |
| engine result -> DTO/repository | Current product truth returns through adapters. |
| repository -> presentation | Concrete transport and DTO types must remain hidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-07-01 | Elevation of Privilege | MockInteractionApi | mitigate | Port-only dependency and no initialize/policy source gate. |
| T-41-07-02 | Tampering | repository result | mitigate | Whole-result mapping and parity tests; no cache/mutation. |
| T-41-07-03 | Information Disclosure | raw request | mitigate | No retained input state and DTO privacy tests inherited from Plan 06. |
</threat_model>

<verification>
Run expected-RED proof, full data tests, parity/delegation assertions, negative business-rule scan, and both semantic/governance verifiers.
</verification>

<success_criteria>
- InteractionEnginePort is the only transport-facing engine seam.
- API/repository layers perform conversion only.
- Five-channel direct/adapter parity passes.
- Initialization and engine internals remain private.
</success_criteria>

<output>
Create `.planning/phases/41-mobile-v2-runnable-vertical-slice/41-07-SUMMARY.md` when done.
</output>
