---
phase: 41-mobile-v2-runnable-vertical-slice
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/phases/41-mobile-v2-runnable-vertical-slice/41-COMMAND-HEALTH.md
  - mobile_v2/pubspec.yaml
  - mobile_v2/pubspec.lock
  - mobile_v2/assets/fixtures/ritual_rooms/.gitkeep
  - mobile_v2/lib/features/ritual_room/domain/models/input_event.dart
  - mobile_v2/lib/features/ritual_room/domain/models/normalized_input.dart
  - mobile_v2/lib/features/ritual_room/domain/models/context_memory.dart
  - mobile_v2/lib/features/ritual_room/domain/models/strategy_decision.dart
  - mobile_v2/lib/features/ritual_room/domain/models/utterance.dart
  - mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart
  - mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart
  - mobile_v2/test/features/ritual_room/domain/models/interaction_contract_test.dart
autonomous: true
requirements: [R058, R059, R060, R067]
user_setup: []
must_haves:
  truths:
    - "The approved D.4.5 prototype and shoes_on illustration exist before package or model work."
    - "Plan 41-01 installs only crypto:^3.0.7, registers both asset directories, and neither installs nor expects flutter_riverpod."
    - "Five raw InputEvent variants, schemaVersion 1, revision 0, immutable ProductSnapshot, and exhaustive advance results exist as Flutter-free contracts."
    - "ProductSnapshot excludes raw observations, consistency receipts, replay evidence, and presentation state."
  artifacts:
    - path: .planning/phases/41-mobile-v2-runnable-vertical-slice/41-COMMAND-HEALTH.md
      provides: "Executable Phase 41 command and fallback map"
    - path: mobile_v2/lib/features/ritual_room/domain/models/input_event.dart
      provides: "Typed ephemeral five-channel event envelope"
    - path: mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart
      provides: "Immutable schema-1 product truth"
    - path: mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart
      provides: "Applied, duplicate, and rejected result contract"
  key_links:
    - from: mobile_v2/lib/features/ritual_room/domain/models/input_event.dart
      to: mobile_v2/lib/features/ritual_room/domain/models/normalized_input.dart
      via: "future NormalizeEngine boundary"
      pattern: "InputEvent"
    - from: mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart
      to: mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart
      via: "latest snapshot carried by results"
      pattern: "ProductSnapshot"
---

<objective>
Lock the approved visual/static gates, core crypto and asset configuration, command health, and immutable Interaction Engine domain contracts.

Purpose: Establish the pure-Dart core before any Riverpod dependency or composition work and remove the superseded flattened snapshot assumption.
Output: Verified gates, crypto/asset configuration, command-health proof, and tested pure-Dart model contracts.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-CONTEXT.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-SCHEMATIC-DESIGN.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-UI-SPEC.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-RESEARCH.md
@mobile_v2/AGENTS.md
@mobile_v2/CODING_STANDARDS.md
@docs/superpowers/plans/2026-06-19-interaction-engine-v1.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce D.4.5, static-asset, crypto, and command gates</name>
  <files>.planning/phases/41-mobile-v2-runnable-vertical-slice/41-COMMAND-HEALTH.md, mobile_v2/pubspec.yaml, mobile_v2/pubspec.lock, mobile_v2/assets/fixtures/ritual_rooms/.gitkeep</files>
  <read_first>
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-SCHEMATIC-DESIGN.md
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-UI-SPEC.md
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-RESEARCH.md
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/assets/prototypes/phase41-d4-5-interaction-engine.png
    @mobile_v2/assets/illustrations/rituals/shoes_on/shoes_on_approved_v1.png
    @mobile_v2/pubspec.yaml
    @mobile_v2/CODING_STANDARDS.md
  </read_first>
  <action>Hard-stop unless both approved artifacts exist. Create the fixture-directory sentinel. Using the verified package audit, add exactly `crypto:^3.0.7`, resolve its lockfile change, and register `assets/fixtures/ritual_rooms/` plus `assets/illustrations/rituals/`. Do not add or expect `flutter_riverpod`; its install, lockfile change, coding-standard decisions, smoke test, and first use belong to Plan 41-08. Add no generator, build_runner, hooks_riverpod, Freezed, networking, persistence, or service-locator package. Write directly runnable focused/final PowerShell commands and SDK fallbacks to `41-COMMAND-HEALTH.md`.</action>
  <acceptance_criteria>
    - File assertion: both approved prototype and illustration paths pass `Test-Path`.
    - Source assertion: `pubspec.yaml` contains `crypto:` and both required asset directories.
    - Source assertion: `pubspec.yaml` contains none of `flutter_riverpod:`, `riverpod_generator`, `build_runner`, `hooks_riverpod`, or `freezed:`.
    - Source assertion: `41-COMMAND-HEALTH.md` names format, analyze, full test, semantic-firewall, and Activation Governor commands.
    - Ordering assertion: no Riverpod dependency or smoke proof is required before Plan 41-08.
  </acceptance_criteria>
  <verify>
    <automated>$paths=@('.planning/phases/41-mobile-v2-runnable-vertical-slice/assets/prototypes/phase41-d4-5-interaction-engine.png','mobile_v2/assets/illustrations/rituals/shoes_on/shoes_on_approved_v1.png'); foreach($p in $paths){ if(!(Test-Path $p)){ throw ('missing approved gate artifact: '+$p) } }</automated>
    <automated>$yaml=Get-Content -Raw mobile_v2/pubspec.yaml; foreach($s in @('crypto:','assets/fixtures/ritual_rooms/','assets/illustrations/rituals/')){ if(-not $yaml.Contains($s)){ throw ('missing pubspec entry: '+$s) } }; foreach($s in @('flutter_riverpod:','riverpod_generator','build_runner','hooks_riverpod','freezed:')){ if($yaml.Contains($s)){ throw ('forbidden pre-Riverpod dependency: '+$s) } }; Push-Location mobile_v2; try { flutter pub get; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>$p='.planning/phases/41-mobile-v2-runnable-vertical-slice/41-COMMAND-HEALTH.md'; if(!(Test-Path $p)){ throw 'missing command health proof' }; $t=Get-Content -Raw $p; foreach($s in @('dart format --output=none --set-exit-if-changed .','flutter analyze','flutter test','verify_mobile_v2_semantic_firewall.dart','verify_activation_governor_contract.dart')){ if(-not $t.Contains($s)){ throw ('missing command: '+$s) } }</automated>
  </verify>
  <done>Both approved gates are present, only crypto is installed, Riverpod remains absent until Plan 41-08, dependency resolution succeeds, and the final command map is executable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 RED: Specify immutable engine contracts</name>
  <files>mobile_v2/test/features/ritual_room/domain/models/interaction_contract_test.dart</files>
  <read_first>
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
    @docs/superpowers/plans/2026-06-19-interaction-engine-v1.md
    @mobile_v2/AGENTS.md
    @mobile_v2/CODING_STANDARDS.md
    @mobile_v2/test/features/ritual_room/domain/models/interaction_contract_test.dart (create)
  </read_first>
  <behavior>
    - Five typed raw event variants exist: reaction_selection, voice_observation, free_text, future_signal, and strategy_preference.
    - ProductSnapshot.initial has schemaVersion 1 and revision 0 with stable identity, anchor, normalized context, compressed memory, strategy, one utterance, and metadata.
    - ProductSnapshot excludes raw payloads, consistency, replay, receipts, navigation, and UI-control state.
    - AdvanceResult covers every locked success and rejection code.
  </behavior>
  <action>Write the tests before production models. Cover immutable collection boundaries, UTC canonical event content, independent schemaVersion/revision, and latestSnapshot on conflicts. This implements R058/R059 ritual-first boundaries, R060 observation-versus-hypothesis separation, and R067 five-channel completeness. Capture the focused test output, require non-zero exit, and require missing-symbol evidence naming `InputEvent`, `ProductSnapshot`, or `AdvanceResult`; the RED verification itself exits 0 only when that expected failure is proven.</action>
  <acceptance_criteria>
    - Test assertion: the source constructs all five InputEvent variants and checks five distinct types.
    - Test assertion: the source checks schemaVersion 1 and revision 0 independently.
    - Test assertion: the source checks immutable collections and absence of raw/infrastructure fields.
    - RED assertion: the focused test fails before production models exist and captured output names a required missing production symbol.
  </acceptance_criteria>
  <verify>
    <automated>$out=Join-Path $env:TEMP '41-01-model-red.out'; $err=Join-Path $env:TEMP '41-01-model-red.err'; Push-Location mobile_v2; try { $p=Start-Process -FilePath flutter -ArgumentList @('test','test/features/ritual_room/domain/models/interaction_contract_test.dart') -Wait -PassThru -NoNewWindow -RedirectStandardOutput $out -RedirectStandardError $err } finally { Pop-Location }; $text=(Get-Content -Raw $out)+(Get-Content -Raw $err); if($p.ExitCode -eq 0){ throw 'expected RED but model contract test passed' }; if($text -notmatch 'InputEvent|ProductSnapshot|AdvanceResult'){ throw 'RED output did not identify a missing production contract symbol' }</automated>
  </verify>
  <done>The expected RED state is mechanically proven to come from absent model contracts, not malformed tests or environment failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 GREEN: Implement pure-Dart immutable models</name>
  <files>mobile_v2/lib/features/ritual_room/domain/models/input_event.dart, mobile_v2/lib/features/ritual_room/domain/models/normalized_input.dart, mobile_v2/lib/features/ritual_room/domain/models/context_memory.dart, mobile_v2/lib/features/ritual_room/domain/models/strategy_decision.dart, mobile_v2/lib/features/ritual_room/domain/models/utterance.dart, mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart, mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart</files>
  <read_first>
    @mobile_v2/test/features/ritual_room/domain/models/interaction_contract_test.dart
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
    @docs/superpowers/plans/2026-06-19-interaction-engine-v1.md
    @mobile_v2/CODING_STANDARDS.md
  </read_first>
  <behavior>
    - Models import only `dart:` libraries and sibling domain files.
    - Raw InputEvent has a stable canonical representation but is never embedded in ProductSnapshot.
    - ProductSnapshot.currentSchemaVersion is 1 and initialization creates revision 0.
    - Exposed collections cannot be mutated by callers.
  </behavior>
  <action>Implement the hand-written immutable contracts from the locked engine plan. Use a sealed typed payload hierarchy behind one stable `InputEvent` envelope. `NormalizedInput` is irreversible/non-verbatim; `ContextMemory` is compressed and interaction-local; `StrategyDecision` describes language policy, never the child; `Utterance` always has one primary line. `ProductSnapshot` is current product truth only. Implement exhaustive `AdvanceResult`/`AdvanceErrorCode`. Add no Flutter/Riverpod imports, DTO behavior, persistence, UI state, child scores, or Phase 42 types.</action>
  <acceptance_criteria>
    - Behavior assertion: the focused model test passes.
    - Analyzer assertion: the domain models directory analyzes with exit code 0.
    - Source assertion: `product_snapshot.dart` contains `currentSchemaVersion` and none of `rawTranscript`, `rawFreeText`, `ConsistencyState`, `ReplayJournal`, or `InputEvent`.
    - Source assertion: model files contain no Flutter or Riverpod imports.
    - Requirement assertion: passing contracts cover R058, R059, R060, and R067 behaviors defined by Task 2.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location mobile_v2; try { flutter test test/features/ritual_room/domain/models/interaction_contract_test.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; dart analyze lib/features/ritual_room/domain/models; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>$files=Get-ChildItem mobile_v2/lib/features/ritual_room/domain/models -Filter *.dart; $hits=$files | Select-String -Pattern 'package:flutter|flutter_riverpod'; if($hits){ $hits | Out-String | Write-Error; throw 'domain model imports framework code' }; $p=Get-Content -Raw mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart; foreach($s in @('rawTranscript','rawFreeText','ConsistencyState','ReplayJournal','InputEvent')){ if($p.Contains($s)){ throw ('ProductSnapshot leaks '+$s) } }</automated>
  </verify>
  <done>The model suite and analyzer pass; immutable ProductSnapshot is product truth and remains free of raw input, infrastructure state, and framework imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry -> mobile_v2 core | The crypto dependency identity and version enter the build before Riverpod work. |
| raw InputEvent -> domain | Sensitive observations enter an ephemeral envelope. |
| domain -> Flutter/Riverpod | Framework imports must not cross into pure contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-01-01 | Tampering | dependency graph | mitigate | Install only verified crypto:^3.0.7, resolve its lockfile, and reject Riverpod or extra packages in this plan. |
| T-41-01-02 | Information Disclosure | ProductSnapshot | mitigate | Tests and source scans reject raw observation and infrastructure fields. |
| T-41-01-03 | Elevation of Privilege | domain imports | mitigate | Analyzer and source scan reject Flutter/Riverpod. |
| T-41-SC | Tampering | pub packages | mitigate | crypto:^3.0.7 must match the 41-RESEARCH package audit; mismatch blocks execution. |
</threat_model>

<verification>
Run asset assertions, package/configuration checks, expected-RED proof, focused model tests, analyzer, and framework/raw-state source scans.
</verification>

<success_criteria>
- D.4.5 prototype and shoes_on illustration are hard gates.
- The core plan installs only crypto; Riverpod is deferred to the first Riverpod plan.
- Five-channel immutable contracts and schema/revision separation are executable.
- ProductSnapshot excludes raw input, consistency, replay, and presentation state.
</success_criteria>

<output>
Create `.planning/phases/41-mobile-v2-runnable-vertical-slice/41-01-SUMMARY.md` when done.
</output>
