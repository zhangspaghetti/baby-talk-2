---
phase: 41-mobile-v2-runnable-vertical-slice
plan: "09"
type: tdd
wave: 5
depends_on: ["41-08"]
files_modified:
  - mobile_v2/lib/app/providers/ritual_room_session_provider.dart
  - mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart
  - mobile_v2/test/app/providers/ritual_room_session_provider_test.dart
  - mobile_v2/test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart
autonomous: true
requirements: [R060, R067]
user_setup: []
must_haves:
  truths:
    - "RitualRoomSessionNotifier is the only mutable Riverpod product-session owner."
    - "RitualRoomUiState carries whole RitualRoomContent plus whole ProductSnapshot and transient status/problem only."
    - "Open loads content then internally initializes once; submit uses the current snapshot revision and replaces the whole snapshot from engine results."
    - "Submitting preserves the last usable snapshot; raw InputEvent is never stored in state."
  artifacts:
    - path: mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart
      provides: "Whole-snapshot immutable UI state union"
    - path: mobile_v2/lib/app/providers/ritual_room_session_provider.dart
      provides: "Sole mutable Ritual Room NotifierProvider"
  key_links:
    - from: mobile_v2/lib/app/providers/ritual_room_session_provider.dart
      to: mobile_v2/lib/features/ritual_room/domain/repositories/interaction_repository.dart
      via: "submit current revision and replace whole snapshot"
      pattern: "expectedRevision"
    - from: mobile_v2/lib/app/providers/ritual_room_session_provider.dart
      to: mobile_v2/lib/features/ritual_room/domain/runtime/interaction_session_initializer.dart
      via: "composition-internal initialize after content load"
      pattern: "initialize"
---

<objective>
TDD the sole mutable Ritual Room session Notifier and whole-snapshot UI state union.

Purpose: Add transient loading/submitting/recoverable orchestration without becoming a second interaction authority or duplicating ProductSnapshot fields.
Output: One NotifierProvider, immutable UI-state union, and focused lifecycle/conflict/stale-operation tests.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
@mobile_v2/AGENTS.md
@mobile_v2/CODING_STANDARDS.md
@docs/superpowers/plans/2026-06-19-interaction-engine-flutter-riverpod.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-08-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Specify whole-snapshot state and sole-Notifier lifecycle</name>
  <files>mobile_v2/test/app/providers/ritual_room_session_provider_test.dart, mobile_v2/test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart</files>
  <read_first>
    @mobile_v2/lib/app/providers/interaction_engine_providers.dart
    @mobile_v2/lib/app/providers/ritual_room_data_providers.dart
    @mobile_v2/lib/app/input/interaction_input_factory.dart
    @mobile_v2/lib/features/ritual_room/domain/models/product_snapshot.dart
    @mobile_v2/lib/features/ritual_room/domain/models/ritual_room_content.dart
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
  </read_first>
  <behavior>
    - UI state variants are idle, loading, ready, submitting, recoverable failure, and load failure using whole room/snapshot references.
    - State declares no duplicate context, memory, strategy, utterance, revision, schemaVersion, or InputEvent fields.
    - openRoom loads content then initializes once for the same active room and ignores stale/disposed async completion.
    - submit uses current snapshot.revision; applied/duplicate replace the whole snapshot; revision/event-ID conflicts use latestSnapshot; pipeline failure retains current snapshot.
    - Programmatic voice/free-text/future-signal/strategy inputs use the same submit command despite hidden UI controls.
  </behavior>
  <action>Write state-shape and ProviderContainer tests first. Use repository/initializer overrides, controlled completers, and deterministic inputs. Explicitly prove R060 interpreted context remains inside ProductSnapshot and R067 all channels share one command. Include exactly-one-Notifier source counting and raw-event-state rejection. Capture RED and require missing `RitualRoomUiState` or `RitualRoomSessionNotifier` evidence.</action>
  <acceptance_criteria>
    - Test assertion: every state variant and whole-snapshot reference is instantiated.
    - Source-shape assertion: forbidden duplicate field names and InputEvent storage are rejected.
    - Test assertion: all applied/duplicate/conflict/failure transitions and stale/disposal behavior are covered.
    - Test assertion: all five input variants use `submit(InputEvent)`.
    - RED assertion: captured output identifies a missing session/state production symbol.
  </acceptance_criteria>
  <verify>
    <automated>$out=Join-Path $env:TEMP '41-09-session-red.out'; $err=Join-Path $env:TEMP '41-09-session-red.err'; Push-Location mobile_v2; try { $p=Start-Process -FilePath flutter -ArgumentList @('test','test/app/providers/ritual_room_session_provider_test.dart','test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart') -Wait -PassThru -NoNewWindow -RedirectStandardOutput $out -RedirectStandardError $err } finally { Pop-Location }; $text=(Get-Content -Raw $out)+(Get-Content -Raw $err); if($p.ExitCode -eq 0){ throw 'expected RED but session tests passed' }; if($text -notmatch 'RitualRoomUiState|RitualRoomSessionNotifier'){ throw 'RED output did not identify a missing session/state symbol' }</automated>
  </verify>
  <done>The expected session RED state is proven against missing production state/Notifier symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN/REFACTOR: Implement one whole-snapshot session Notifier</name>
  <files>mobile_v2/lib/app/providers/ritual_room_session_provider.dart, mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart</files>
  <read_first>
    @mobile_v2/test/app/providers/ritual_room_session_provider_test.dart
    @mobile_v2/test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart
    @mobile_v2/lib/app/providers/ritual_room_data_providers.dart
    @mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart
    @docs/superpowers/plans/2026-06-19-interaction-engine-flutter-riverpod.md
  </read_first>
  <action>Implement a sealed immutable UI-state union carrying only whole room/snapshot references plus transient problem/cause. Implement exactly one `NotifierProvider<RitualRoomSessionNotifier, RitualRoomUiState>`. `openRoom` loads content then invokes the internal initializer once, guarded by an operation epoch and disposal invalidation. `submit(InputEvent)` preserves the current snapshot while pending, sends snapshot interactionId/revision to the repository, and replaces the whole snapshot from Applied, DuplicateIgnored, or latest conflict results; retain current snapshot for failures without latestSnapshot. Do not derive context/memory/strategy/utterance, store raw input for retry, generate new IDs, inspect CapabilityMask, or add a parallel controller/ViewModel.</action>
  <acceptance_criteria>
    - Behavior assertion: session and UI-state tests pass.
    - Source assertion: exactly one `NotifierProvider` exists under `mobile_v2/lib/app/providers`.
    - Source assertion: UI-state file declares no duplicate ProductSnapshot subfields and no InputEvent field.
    - Behavior assertion: stale/disposed operations do not update state and same-room open does not reinitialize.
    - Behavior assertion: all five programmatic channels pass through one submit method.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location mobile_v2; try { flutter test test/app/providers/ritual_room_session_provider_test.dart test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>$count=(Get-ChildItem mobile_v2/lib/app/providers -Recurse -Filter *.dart | Select-String -Pattern 'NotifierProvider').Count; if($count -ne 1){ throw ('expected exactly one NotifierProvider, found '+$count) }; $state=Get-Content -Raw mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart; foreach($s in @('final InteractionContext','final ContextMemory','final StrategyDecision','final Utterance','final int revision','final int schemaVersion','final InputEvent')){ if($state.Contains($s)){ throw ('duplicated or raw state field: '+$s) } }</automated>
  </verify>
  <done>The sole Notifier passes lifecycle, conflict-overwrite, whole-snapshot, stale-operation, and five-channel tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository result -> Notifier | Engine last-write truth enters transient UI orchestration. |
| raw InputEvent -> submit | Sensitive input exists only for the active request. |
| async completion -> state | Stale/disposed operations must not overwrite current state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-09-01 | Tampering | session snapshot | mitigate | Whole-snapshot replacement tests; no field-level mutation. |
| T-41-09-02 | Information Disclosure | raw event state | mitigate | State shape/source tests reject InputEvent storage. |
| T-41-09-03 | Denial of Service | stale async operation | mitigate | Epoch/disposal guards and controlled-completer tests. |
| T-41-09-04 | Elevation of Privilege | duplicate state owner | mitigate | Exactly-one NotifierProvider gate and no parallel controller. |
</threat_model>

<verification>
Run expected-RED proof, session/state tests, exact Notifier count, forbidden-field scan, stale/disposal assertions, and five-channel submit tests.
</verification>

<success_criteria>
- One Notifier owns transient Ritual Room session state.
- ProductSnapshot remains whole and authoritative.
- Raw inputs are not retained.
- Engine conflict snapshots win without UI derivation.
</success_criteria>

<output>
Create `.planning/phases/41-mobile-v2-runnable-vertical-slice/41-09-SUMMARY.md` when done.
</output>
