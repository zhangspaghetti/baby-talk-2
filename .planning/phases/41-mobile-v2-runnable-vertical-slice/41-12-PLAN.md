---
phase: 41-mobile-v2-runnable-vertical-slice
plan: "12"
type: tdd
wave: 8
depends_on: ["41-11"]
files_modified:
  - mobile_v2/lib/features/ritual_room/domain/repositories/interaction_outcome_unknown_exception.dart
  - mobile_v2/lib/app/providers/ritual_room_session_provider.dart
  - mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart
  - mobile_v2/lib/app/baby_talk_app.dart
  - mobile_v2/lib/features/ritual_room/presentation/screens/ritual_room_screen.dart
  - mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_context_input_tray.dart
  - mobile_v2/test/app/providers/ritual_room_session_provider_test.dart
  - mobile_v2/test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart
  - mobile_v2/test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart
  - mobile_v2/test/features/ritual_room/presentation/ritual_room_accessibility_test.dart
autonomous: true
gap_closure: true
requirements: [R060, R067]
user_setup: []
must_haves:
  truths:
    - "A second reaction tap while submitting, awaiting unknown-outcome reconciliation, or retrying is a complete no-op: no InputEvent/eventId allocation, repository call, queue, merge, or delayed submission."
    - "Only InteractionOutcomeUnknownException preserves the notifier-private command; retry replays the identical InputEvent, eventId, interactionId, and expectedRevision."
    - "Applied, DuplicateIgnored, every AdvanceRejected result, and every non-unknown exception clear the private command; a later deliberate reaction creates a new event."
    - "Submitting, unknown-outcome, and retrying UI states disable every reaction-producing control, including choices in an already-open additional-reaction modal route, while preserving the selected reaction, current utterance, playback, and quiet exit."
    - "InputEvent, eventId, interactionId, expectedRevision, and the command envelope never enter BabyTalkApp, RitualRoomScreen, or public RitualRoomUiState."
    - "The existing engine receipt-before-revision contract still returns duplicate_ignored for the old expectedRevision after the original event committed."
  artifacts:
    - path: mobile_v2/lib/features/ritual_room/domain/repositories/interaction_outcome_unknown_exception.dart
      provides: "Explicit post-dispatch unknown-outcome classification"
      exports: ["InteractionOutcomeUnknownException", "InteractionOutcomeUnknownReason"]
    - path: mobile_v2/lib/app/providers/ritual_room_session_provider.dart
      provides: "Single-flight reaction admission and notifier-private exact-command reconciliation"
      contains: "retryPendingEvent"
    - path: mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart
      provides: "Command-free submitting and unknown-outcome projection state"
      contains: "RitualRoomUnknownOutcome"
    - path: mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_context_input_tray.dart
      provides: "Disabled and selected reaction-control projection with a tray-owned reactive gate observed by an already-open modal route"
    - path: mobile_v2/test/app/providers/ritual_room_session_provider_test.dart
      provides: "Controlled-completer single-flight and commit-succeeded/response-lost reconciliation regressions"
    - path: mobile_v2/test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart
      provides: "Locked controls, unknown-outcome retry UI, and app ownership regression coverage"
    - path: mobile_v2/test/features/ritual_room/presentation/ritual_room_accessibility_test.dart
      provides: "Updated required screen callback and reconciliation-state accessibility coverage"
  key_links:
    - from: mobile_v2/lib/app/baby_talk_app.dart
      to: mobile_v2/lib/app/providers/ritual_room_session_provider.dart
      via: "reaction ID only through submitReaction; retry intent only through retryPendingEvent"
      pattern: "submitReaction|retryPendingEvent"
    - from: mobile_v2/lib/app/providers/ritual_room_session_provider.dart
      to: mobile_v2/lib/features/ritual_room/domain/repositories/interaction_repository.dart
      via: "one retained private command submitted with its captured interactionId and expectedRevision"
      pattern: "_PendingInteractionCommand"
    - from: mobile_v2/lib/app/providers/ritual_room_session_provider.dart
      to: mobile_v2/lib/features/ritual_room/domain/repositories/interaction_outcome_unknown_exception.dart
      via: "only explicit unknown-outcome exceptions preserve retry capability"
      pattern: "on InteractionOutcomeUnknownException"
    - from: mobile_v2/lib/features/ritual_room/presentation/screens/ritual_room_screen.dart
      to: mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_context_input_tray.dart
      via: "submitting/unknown/retrying state maps to enabled=false and selectedReactionId"
      pattern: "selectedReactionId|enabled"
    - from: mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_context_input_tray.dart
      to: "_AdditionalReactionChoicesSheet"
      via: "didUpdateWidget publishes enabled/selected changes through the tray-owned ValueNotifier; every modal callback uses guarded dispatch and pops only after the live projection accepts emission"
      pattern: "ValueNotifier<_ReactionControlProjection>|ValueListenableBuilder<_ReactionControlProjection>|_dispatchReaction"
---

<objective>
Close the two Phase 41 reaction-lifecycle blockers with notifier-owned single-flight admission and exact unknown-outcome reconciliation.

Purpose: Prevent rapid taps from allocating duplicate events or hiding a successful snapshot, and make commit-succeeded/response-lost recovery replay the exact immutable command so the engine resolves it through duplicate_ignored.
Output: Explicit unknown-outcome classification, a private retained command envelope, locked reconciliation UI, and controlled regression tests.
</objective>

<execution_context>
@C:/Users/zhang/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/zhang/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-CONTEXT.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-VERIFICATION.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-INTERACTION-ENGINE-CONTRACT.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-VALIDATION.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-UI-SPEC.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-PATTERNS.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-RESEARCH.md
@docs/superpowers/specs/2026-06-21-phase-41-reaction-reconciliation-design.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-09-SUMMARY.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-10-SUMMARY.md
@.planning/phases/41-mobile-v2-runnable-vertical-slice/41-11-SUMMARY.md
@mobile_v2/AGENTS.md
@mobile_v2/CODING_STANDARDS.md
</context>

## Multi-Source Coverage Audit

| Source | ID | Required item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Runnable Ritual Room preserves engine lifecycle, conflict, idempotency, and reaction-only projection contracts | 41-12 | COVERED | Closes the two verification blockers only |
| REQ | R060 | Context evidence remains non-diagnostic and raw/private command data stays outside public UI state | 41-12 | COVERED | Public-state/source firewall plus full semantic verifier |
| REQ | R067 | Reaction path remains a correct projection over the five-channel evolving Interaction Engine | 41-12 | COVERED | Single-flight UI path; existing five-channel submit and engine suites remain required |
| REQ | R058 | Family-micro-ritual-first surface remains unchanged | 41-12 | PRESERVED | Full screen tests and semantic firewall; no navigation/product expansion |
| REQ | R059 | Stable micro-ritual identity and one current utterance remain unchanged | 41-12 | PRESERVED | Locked states keep the current snapshot readable |
| REQ | R063 | Activation remains Governor-gated | 41-12 | PRESERVED | Activation Governor verifier remains a final gate |
| REQ | R064 | No scoring/check-in/production Garden transition | 41-12 | PRESERVED | No Garden state or copy changes; verifier remains a final gate |
| REQ | R065 | Explore and Activate remain distinct | 41-12 | PRESERVED | No activation or recommendation authority is added |
| RESEARCH | A1 / current architecture | One whole-ProductSnapshot session Notifier remains the sole mutable presentation authority | 41-12 | COVERED | Command retention is private inside the existing Notifier; no second owner |
| RESEARCH | D-07, D-14, D-15 | Direct single-room surface; no navigation/progression/scoring expansion | 41-12 | PRESERVED | Existing app/screen shape remains; final source/semantic gates run |
| CONTEXT | D.4.5 | Preserve current utterance while pending; reaction-only projection; playback and quiet exit remain usable | 41-12 | COVERED | Adds locked reaction controls without a global screen freeze |
| LOCKED GAP DESIGN | Single-flight | Guard before event creation; ignored taps allocate nothing and call nothing | 41-12 | COVERED | Controlled completer asserts factory/repository call counts |
| LOCKED GAP DESIGN | Exact reconciliation | Unknown outcome replays the same InputEvent/eventId/interactionId/expectedRevision | 41-12 | COVERED | Real-engine commit-then-lost-response test resolves duplicate_ignored |
| LOCKED GAP DESIGN | Outcome classification | Only explicit unknown-outcome exceptions retain the command | 41-12 | COVERED | All authoritative and non-unknown outcomes clear |
| LOCKED GAP DESIGN | Lifecycle | Room switch, reload, and disposal abandon local retry without replacement submission | 41-12 | COVERED | Provider lifecycle tests assert no replacement event/call |

Excluded as explicitly out of scope: future-signal value normalization, hardcoded visible copy cleanup, numeric snapshot ranges, mutable capability sets, duplicate room-content reads, and the broad `InteractionEngine` catch. Non-unknown exceptions may clear the notifier-private command only to protect retry semantics; this plan must not modify `interaction_engine.dart` or claim the broad-catch warning is fixed.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 RED: Specify single-flight admission, exact reconciliation, and locked UI behavior</name>
  <files>mobile_v2/test/app/providers/ritual_room_session_provider_test.dart, mobile_v2/test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart, mobile_v2/test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart, mobile_v2/test/features/ritual_room/presentation/ritual_room_accessibility_test.dart</files>
  <read_first>
    @docs/superpowers/specs/2026-06-21-phase-41-reaction-reconciliation-design.md
    @mobile_v2/lib/app/providers/ritual_room_session_provider.dart
    @mobile_v2/lib/app/providers/interaction_engine_providers.dart
    @mobile_v2/lib/app/input/interaction_input_factory.dart
    @mobile_v2/lib/features/ritual_room/domain/engine/interaction_engine.dart
    @mobile_v2/lib/features/ritual_room/domain/repositories/interaction_repository.dart
    @mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart
    @mobile_v2/lib/features/ritual_room/presentation/screens/ritual_room_screen.dart
    @mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_context_input_tray.dart
    @mobile_v2/test/helpers/interaction_test_doubles.dart
  </read_first>
  <behavior>
    - Controlled-completer fast taps: call `submitReaction('not_ready')` twice before completing the first repository future; `_CountingEventIdGenerator.calls == 1`, repository advance count is 1, only the first selected reaction is retained, no queued reaction appears, and the eventual `AdvanceApplied` snapshot becomes `RitualRoomReady`.
    - Commit-succeeded/response-lost: `_CommitThenLoseResponseRepository` delegates the first command to a real `InteractionEngineHarness`, discards its `AdvanceApplied`, then throws `InteractionOutcomeUnknownException(reason: responseLostAfterDispatch)`; `retryPendingEvent()` delegates the exact same command and receives `AdvanceDuplicateIgnored`.
    - Exact envelope assertions compare object identity for `InputEvent` and equality for `eventId`, `interactionId`, and the old `expectedRevision`; the event-ID generator remains at one call and the returned authoritative snapshot becomes Ready.
    - Repeated reaction taps and repeated retry taps during Submitting/UnknownOutcome/isRetrying are no-ops. A second unknown outcome retains the same command for another retry.
    - Every authoritative result, including revision conflict and pipelineFailed, clears the command. Every non-unknown exception clears it. A later deliberate reaction after either case creates a new event ID.
    - Room switch, explicit reload, and ProviderContainer disposal clear local retry capability and make no replacement repository call or event allocation.
    - Public state includes `RitualRoomSubmitting.selectedReaction` and `RitualRoomUnknownOutcome(room, snapshot, selectedReaction, isRetrying)` but declares no InputEvent, eventId, interactionId, expectedRevision, or command field.
    - Screen tests prove Submitting, UnknownOutcome, and retrying disable both inline reaction buttons and `ritual-more-reactions`, preserve selected semantics, show the exact unknown copy and `ritual-unknown-outcome-retry`, disable retry while retrying, and keep listen/quiet-exit callbacks usable.
    - Modal-route regression pumps Ready through a local `ValueNotifier<RitualRoomUiState>`, opens `ritual-reaction-sheet`, updates the same mounted screen/tray to `RitualRoomSubmitting`, and proves every `OutlinedButton` descendant of the sheet has `onPressed == null`; real taps on every sheet choice emit no reaction and do not close the sheet.
    - The same modal regression returns the mounted screen/tray to Ready and proves the open sheet re-enables against the current callback exactly once, then reopens/closes the sheet and disposes the harness with no stale callback, duplicate listener, disposed-notifier assertion, or `tester.takeException()`.
    - Accessibility tests supply the new required `onRetryPendingEvent` callback and prove the unknown-outcome message/retry action remain semantically reachable without weakening existing 390x844 or 48x48 checks.
    - Replace the old screen-test framing that called an authoritative pipeline failure a same-event retry. Pipeline failure is authoritative; a later tap is a new command. Same-event replay is asserted only for `InteractionOutcomeUnknownException`.
  </behavior>
  <action>Write the provider, state, screen, and accessibility regressions before production changes. Extend the provider test with exact helpers named `_CountingEventIdGenerator`, `_RecordedAdvanceCall`, and `_CommitThenLoseResponseRepository`; use `InteractionEngineHarness` so the first attempt actually commits and the second exact attempt returns duplicate_ignored through receipt-before-revision ordering. Override `interactionEventIdGeneratorProvider`, not production randomness. Add state source assertions that scan only public app/screen/state files, allowing the private envelope solely in `ritual_room_session_provider.dart`. Add widget assertions against disabled `ButtonStyleButton.onPressed`, selected semantics, the exact approved unknown-outcome copy, callable playback/quiet exit, and the new required screen retry callback. Add a focused screen test named `already-open reaction sheet follows reaction lock and releases its listener lifecycle`: use a local `ValueNotifier<RitualRoomUiState>` plus `ValueListenableBuilder` so the same `RitualRoomScreen` and `RitualContextInputTray` stay mounted; pump Ready, open `ritual-reaction-sheet`, publish Submitting with a selected reaction, inspect and tap every sheet `OutlinedButton`, then publish Ready while the sheet remains open and prove one enabled choice invokes the current callback exactly once and closes only the sheet. Reopen the sheet, close it without selection, dispose the harness/notifier, and require no extra callback or Flutter exception. Do not alter tests for any of the six excluded warnings.</action>
  <acceptance_criteria>
    - RED command exits non-zero and names at least one missing symbol from `InteractionOutcomeUnknownException`, `submitReaction`, `retryPendingEvent`, `RitualRoomUnknownOutcome`, or the new disabled/selected tray parameters.
    - Provider test explicitly asserts one factory allocation and one repository call for two rapid reaction taps.
    - Real-engine reconciliation test explicitly asserts first-call commit, lost response, exact replay, duplicate_ignored, Ready revision 1, and no new event ID.
    - Tests cover repeated unknown outcome, all authoritative/non-unknown clearing behavior, and room switch/reload/dispose abandonment.
    - Screen/state tests assert no public command-envelope field or app-layer InputEvent/factory access.
    - The already-open-sheet regression proves disabled visual/callback state updates without dismissing the modal, disabled taps emit nothing, Ready re-enables the same sheet exactly once, and closing/disposal leaves no stale listener, callback, or notifier exception.
  </acceptance_criteria>
  <verify>
    <automated>$out=Join-Path $env:TEMP '41-12-open-sheet-red.out'; $err=Join-Path $env:TEMP '41-12-open-sheet-red.err'; Push-Location mobile_v2; try { $p=Start-Process -FilePath flutter -ArgumentList @('test','--no-pub','test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart','--plain-name','already-open reaction sheet follows reaction lock and releases its listener lifecycle') -Wait -PassThru -NoNewWindow -RedirectStandardOutput $out -RedirectStandardError $err } finally { Pop-Location }; if($p.ExitCode -eq 0){ throw 'expected RED but the already-open-sheet lock regression passed' }</automated>
    <automated>$out=Join-Path $env:TEMP '41-12-reconciliation-red.out'; $err=Join-Path $env:TEMP '41-12-reconciliation-red.err'; Push-Location mobile_v2; try { $p=Start-Process -FilePath flutter -ArgumentList @('test','--no-pub','test/app/providers/ritual_room_session_provider_test.dart','test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart','test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart','test/features/ritual_room/presentation/ritual_room_accessibility_test.dart') -Wait -PassThru -NoNewWindow -RedirectStandardOutput $out -RedirectStandardError $err } finally { Pop-Location }; $text=(Get-Content -Raw $out)+(Get-Content -Raw $err); if($p.ExitCode -eq 0){ throw 'expected RED but reconciliation tests passed' }; if($text -notmatch 'InteractionOutcomeUnknownException|submitReaction|retryPendingEvent|RitualRoomUnknownOutcome|selectedReactionId|enabled|onRetryPendingEvent'){ throw 'RED output did not identify a missing reconciliation symbol' }</automated>
  </verify>
  <done>The expected RED state proves both blockers and their lifecycle/UI boundaries fail before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 GREEN: Implement explicit unknown-outcome classification and notifier-private command replay</name>
  <files>mobile_v2/lib/features/ritual_room/domain/repositories/interaction_outcome_unknown_exception.dart, mobile_v2/lib/app/providers/ritual_room_session_provider.dart, mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart</files>
  <read_first>
    @mobile_v2/test/app/providers/ritual_room_session_provider_test.dart
    @mobile_v2/test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart
    @mobile_v2/lib/features/ritual_room/domain/models/advance_result.dart
    @mobile_v2/lib/features/ritual_room/domain/models/input_event.dart
    @mobile_v2/lib/features/ritual_room/domain/repositories/interaction_repository.dart
    @mobile_v2/lib/app/providers/interaction_engine_providers.dart
    @mobile_v2/CODING_STANDARDS.md
  </read_first>
  <action>Create `InteractionOutcomeUnknownReason` with exactly `timeoutAfterDispatch`, `responseLostAfterDispatch`, and `connectionClosedAfterDispatch`, plus final immutable `InteractionOutcomeUnknownException({required reason, cause})`. In `RitualRoomSessionNotifier`, add private `_PendingInteractionCommand` with exactly `InputEvent input`, `String interactionId`, `int expectedRevision`, and `String? selectedReaction`; retain at most one instance. Add `submitReaction(String selected)` as the only UI reaction entry: check the single-flight/state guard first, then call `interactionInputFactoryProvider.reaction(selected)`, capture snapshot interactionId/revision, save the complete command, enter `RitualRoomSubmitting`, and call the repository. Keep `submit(InputEvent)` only as the existing programmatic non-UI R067 seam and route it through the same command guard; BabyTalkApp must no longer use it. Add `retryPendingEvent()` that runs only from `RitualRoomUnknownOutcome(isRetrying: false)`, sets `isRetrying: true`, and resubmits the retained command without rebasing or regenerating any field. Catch only `InteractionOutcomeUnknownException` into `RitualRoomUnknownOutcome` while retaining the command. Clear before publishing every authoritative `AdvanceResult` or non-unknown exception; applied/duplicate become Ready, rejected becomes RecoverableFailure using `latestSnapshot ?? priorSnapshot`. A repeated unknown outcome returns to `isRetrying: false` with the same command. Add `reloadRoom(String ritualRoomId)` as an explicit stream replacement; room switch, reload, and disposal clear the command and invalidate old completions without submitting a replacement. Update `_usableSession` so Submitting and UnknownOutcome never admit new commands. Add `selectedReaction` to Submitting and add command-free `RitualRoomUnknownOutcome` with only room, snapshot, selectedReaction, and isRetrying. Do not expose a getter for the private command. Do not change `InteractionEngine`, repository retry policy, or broad exception handling.</action>
  <acceptance_criteria>
    - Exact exception enum/class and private command fields match the approved design.
    - Guard executes before `InteractionInputFactory.reaction`, so ignored taps allocate no event ID.
    - Retry passes the retained command's same InputEvent object, interactionId, and expectedRevision.
    - Only explicit unknown-outcome exceptions preserve the command; all authoritative and other exception paths clear it.
    - Public state exposes selected reaction/retrying status but no command-envelope data.
    - Focused provider/state tests pass while the full five-channel `submit(InputEvent)` contract remains intact.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location mobile_v2; try { flutter test --no-pub test/app/providers/ritual_room_session_provider_test.dart test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>$notifier=Get-Content -Raw 'mobile_v2/lib/app/providers/ritual_room_session_provider.dart'; foreach($required in @('final class _PendingInteractionCommand','Future<void> submitReaction','Future<void> retryPendingEvent','on InteractionOutcomeUnknownException')){ if(-not $notifier.Contains($required)){ throw ('notifier missing '+$required) } }; $state=Get-Content -Raw 'mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart'; foreach($forbidden in @('final InputEvent','final String eventId','final String interactionId','final int expectedRevision','_PendingInteractionCommand')){ if($state.Contains($forbidden)){ throw ('public state leaks private command field '+$forbidden) } }</automated>
  </verify>
  <done>The notifier admits one command, classifies only explicit unknown outcomes as retryable, and exactly replays its private envelope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 GREEN/REFACTOR: Wire intent-only app callbacks, lock reaction controls, and run closure gates</name>
  <files>mobile_v2/lib/app/baby_talk_app.dart, mobile_v2/lib/features/ritual_room/presentation/screens/ritual_room_screen.dart, mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_context_input_tray.dart</files>
  <read_first>
    @mobile_v2/test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart
    @mobile_v2/lib/app/providers/ritual_room_session_provider.dart
    @mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart
    @mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_context_input_tray.dart
    @mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_listen_control.dart
    @mobile_v2/lib/features/ritual_room/presentation/widgets/ritual_reassurance.dart
    @mobile_v2/test/features/ritual_room/interaction_engine_contract_test.dart
    @mobile_v2/test/features/ritual_room/domain/engine/interaction_engine_test.dart
    @.planning/phases/41-mobile-v2-runnable-vertical-slice/41-UI-SPEC.md
  </read_first>
  <action>Remove `interactionInputFactoryProvider` and every InputEvent concern from `BabyTalkApp`. Wire reaction IDs to `RitualRoomSessionNotifier.submitReaction`, unknown-outcome retry intent to `retryPendingEvent`, and load retry to `reloadRoom(_ritualRoomId)`. Add required `RitualRoomScreen.onRetryPendingEvent` and update its direct accessibility-test constructions. Project `RitualRoomSubmitting` and `RitualRoomUnknownOutcome` through one room surface; derive `reactionsLocked`, `selectedReaction`, and `isRetrying` only from public state. Add the exact unknown copy `刚才这次没有确认成功，可以再试一次` and a 48x48-min `ritual-unknown-outcome-retry` action disabled while retrying. Convert `RitualContextInputTray` to a `StatefulWidget` while preserving backward-compatible constructor parameters `bool enabled = true` and `String? selectedReactionId`. Its `_RitualContextInputTrayState` must own one `ValueNotifier<_ReactionControlProjection>` containing the current enabled flag and selected reaction ID; initialize it from the widget, update it in `didUpdateWidget`, and pass it to a private `_AdditionalReactionChoicesSheet` that renders choices through `ValueListenableBuilder<_ReactionControlProjection>`. Inline and launcher callbacks are null when disabled. Modal choices are also visually/semantically disabled or selected from the live projection. Implement `_dispatchReaction(String choiceId)` as a guarded boolean operation: return false without side effects unless the State is mounted and the notifier's current projection is enabled; otherwise invoke the current `widget.onReactionSelected` exactly once and return true. A modal choice may call `Navigator.of(sheetContext).pop()` only when `_dispatchReaction` returns true, so a stale enabled build cannot pop or emit after the live lock changes and a parent callback replacement is observed through the current widget. Track whether this tray's sheet future is open. On tray disposal, synchronously publish disabled state so the separate modal route cannot emit; dispose the notifier immediately when no sheet is open, otherwise defer notifier disposal until that exact `showModalBottomSheet` future completes. Normal sheet close must detach its `ValueListenableBuilder` listener, reset the open flag, and keep the notifier reusable for a later sheet while the tray remains mounted. Do not dismiss routes or call a root navigator. Keep `RitualListenControl` and quiet exit callbacks active in every snapshot-bearing state. REFACTOR only within the ten declared files, run formatting, and retain the existing receipt-before-revision tests where an event committed at revision 0 is replayed with expectedRevision 0 after the snapshot advanced. Do not move existing visible copy into the content model, normalize future-signal values, constrain numeric ranges, copy capability sets, deduplicate room reads, or edit the broad engine catch.</action>
  <acceptance_criteria>
    - BabyTalkApp contains `submitReaction` and `retryPendingEvent` but no InputEvent, eventId, expectedRevision, or `interactionInputFactoryProvider`.
    - Submitting, UnknownOutcome, and retrying screen tests find all reaction-producing buttons disabled and the selected reaction still observable.
    - An additional-reaction sheet opened in Ready reacts in place to Submitting/UnknownOutcome/retrying projection changes: all sheet choices become disabled, tap-time guards prevent emission even before a scheduled modal rebuild, and returning to Ready re-enables the same sheet without duplicating callbacks.
    - Closing or disposing the tray with a sheet open disables emission first and disposes the tray-owned notifier only after the sheet listener detaches; reopening/closing produces no stale callback, duplicate listener, or disposed-notifier assertion.
    - UnknownOutcome shows the exact approved copy and retry action; retrying disables only retry/reaction production, not playback or quiet exit.
    - Focused provider/state/screen tests, interaction engine contract tests, and the receipt-before-revision engine test all pass.
    - Format, analyze, complete `mobile_v2` tests, semantic firewall, and Activation Governor verifier all exit 0.
    - Source scans confirm the command envelope exists only in the Notifier and no excluded warning area was modified.
  </acceptance_criteria>
  <verify>
    <automated>Push-Location mobile_v2; try { flutter test --no-pub test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart --plain-name "already-open reaction sheet follows reaction lock and releases its listener lifecycle"; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>Push-Location mobile_v2; try { flutter test --no-pub test/app/providers/ritual_room_session_provider_test.dart test/features/ritual_room/presentation/state/ritual_room_ui_state_test.dart test/features/ritual_room/presentation/screens/ritual_room_screen_test.dart test/features/ritual_room/presentation/ritual_room_accessibility_test.dart test/features/ritual_room/interaction_engine_contract_test.dart test/features/ritual_room/domain/engine/interaction_engine_test.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>Push-Location mobile_v2; try { dart format --output=none --set-exit-if-changed lib test; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; flutter analyze --no-pub; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; flutter test --no-pub; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; dart run ../tool/verify_mobile_v2_semantic_firewall.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE }; dart run ../tool/verify_activation_governor_contract.dart; if($LASTEXITCODE -ne 0){ exit $LASTEXITCODE } } finally { Pop-Location }</automated>
    <automated>$app=Get-Content -Raw 'mobile_v2/lib/app/baby_talk_app.dart'; foreach($forbidden in @('InputEvent','interactionInputFactoryProvider','eventId','expectedRevision')){ if($app.Contains($forbidden)){ throw ('BabyTalkApp owns forbidden command detail '+$forbidden) } }; $public=(Get-Content -Raw 'mobile_v2/lib/features/ritual_room/presentation/state/ritual_room_ui_state.dart')+(Get-Content -Raw 'mobile_v2/lib/features/ritual_room/presentation/screens/ritual_room_screen.dart'); foreach($forbidden in @('final InputEvent','final String eventId','final String interactionId','final int expectedRevision','_PendingInteractionCommand')){ if($public.Contains($forbidden)){ throw ('public UI leaks command detail '+$forbidden) } }; $engine=Get-Content -Raw 'mobile_v2/lib/features/ritual_room/domain/engine/interaction_engine.dart'; if($engine.IndexOf('findByEventId') -lt 0 -or $engine.IndexOf('expectedRevision !=') -lt 0 -or $engine.IndexOf('findByEventId') -gt $engine.IndexOf('expectedRevision !=')){ throw 'receipt lookup no longer precedes expected-revision validation' }</automated>
  </verify>
  <done>Reaction controls are single-flight, unknown outcomes reconcile by exact replay, public UI remains command-blind, and all Phase 41 safety gates pass.</done>
</task>

</tasks>

## Artifacts this phase produces

### New file

- `mobile_v2/lib/features/ritual_room/domain/repositories/interaction_outcome_unknown_exception.dart`

### New production symbols and fields

- `InteractionOutcomeUnknownReason`
- `InteractionOutcomeUnknownException`
- `RitualRoomUnknownOutcome`
- `RitualRoomSubmitting.selectedReaction`
- `RitualRoomUnknownOutcome.selectedReaction`
- `RitualRoomUnknownOutcome.isRetrying`
- `RitualRoomSessionNotifier.submitReaction(String selected)`
- `RitualRoomSessionNotifier.retryPendingEvent()`
- `RitualRoomSessionNotifier.reloadRoom(String ritualRoomId)`
- `_PendingInteractionCommand`
- `RitualRoomScreen.onRetryPendingEvent`
- `RitualContextInputTray.enabled`
- `RitualContextInputTray.selectedReactionId`
- `_RitualContextInputTrayState._dispatchReaction(String choiceId)`
- `_RitualContextInputTrayState`
- `_ReactionControlProjection`
- `_AdditionalReactionChoicesSheet`

### New test-only symbols

- `_CountingEventIdGenerator`
- `_RecordedAdvanceCall`
- `_CommitThenLoseResponseRepository`

No new package, provider, engine result, DTO, API endpoint, persistence artifact, route, or production Garden artifact is created.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| reaction gesture -> Notifier | Untrusted repeated user input must pass the single-flight guard before identity allocation. |
| Notifier -> repository/engine | The private idempotency command crosses the data boundary once per attempt and must replay byte-for-byte logically unchanged. |
| result/exception -> UI state | Only authoritative results clear uncertainty; only explicit post-dispatch unknown outcomes preserve retry capability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-41-12-01 | Tampering | idempotency envelope | mitigate | Private immutable `_PendingInteractionCommand`; identity/equality assertions for all four envelope fields. |
| T-41-12-02 | Replay | unknown-outcome retry | mitigate | Permit exact replay only for `InteractionOutcomeUnknownException`; engine receipt-before-revision returns duplicate_ignored. |
| T-41-12-03 | Denial of Service | rapid taps/retry taps | mitigate | Guard before event allocation; controlled-completer tests prove one factory call and one repository call. |
| T-41-12-04 | Information Disclosure | public app/UI state | mitigate | Source/state tests reject InputEvent, eventId, interactionId, expectedRevision, and command-envelope leakage. |
</threat_model>

<verification>
1. Prove RED against missing reconciliation symbols.
2. Pass focused notifier/state/screen regressions, including controlled fast taps and real-engine commit-then-response-lost duplicate reconciliation.
3. Pass existing interaction engine contract and receipt-before-revision tests.
4. Run `dart format --output=none --set-exit-if-changed lib test`, `flutter analyze --no-pub`, and complete `flutter test --no-pub` from `mobile_v2`.
5. Run `dart run ../tool/verify_mobile_v2_semantic_firewall.dart` and `dart run ../tool/verify_activation_governor_contract.dart`.
6. Run source firewalls proving app/UI command blindness and receipt lookup ordering.
</verification>

<success_criteria>
- Two rapid reaction taps allocate one event ID and make one repository call.
- A committed request with a lost response enters UnknownOutcome and exact retry resolves through AdvanceDuplicateIgnored with the authoritative snapshot.
- Repeated retry taps and repeated reaction taps while locked are no-ops.
- Authoritative results and non-unknown exceptions clear retry capability; later deliberate input receives a new event ID.
- Public app/UI state never contains or accepts the private command envelope.
- Room switch, reload, and disposal abandon local retry without generating a replacement event.
- All focused, full-package, semantic-firewall, and Activation Governor gates pass.
- The six verification warnings remain explicitly out of scope and unclaimed.
</success_criteria>

<output>
Create `.planning/phases/41-mobile-v2-runnable-vertical-slice/41-12-SUMMARY.md` when done.
</output>
