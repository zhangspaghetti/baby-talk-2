---
phase: "05"
plan: "01"
type: "implementation"
---

<objective>
创建 KG Schema + 领域记录 + JdbcTemplate Repository + 单元测试
</objective>

<context>
Phase: 05 (s05) — Milestone: MemPalace 育儿知识宫殿
</context>

<must_haves>

- V14 迁移文件语法正确，包含 pg_trgm 扩展、4 张表、所有 CHECK 约束和索引
- 4 个 Java record 映射表的所有列
- 4 个 Repository 覆盖必需的 CRUD 方法
- 至少 2 个集成测试类验证 repository 核心操作
- `mvn compile test-compile` 编译通过

</must_haves>
