---
phase: "17"
plan: "02"
type: "implementation"
---

<objective>
组装 ProLayout shell、默认 landing 与导航过滤
</objective>

<context>
Phase: 17 (s04) — Milestone: Admin 管理后台与后端多模块重构

在 route catalog 就绪后，把现有 workspace-switcher proof 改成真实的 ProLayout shell：左侧导航、顶部 identity/logout、role-aware default landing，以及 `Overview` / `Users` / `Knowledge Ops` placeholders + 已有 `Mentor & Safety` / `Distribution Stats` workspace 的统一挂载。这个任务先把用户可见 IA 建起来，确保成功登录后进入真实 module route，而不是 `/protected` stub。
</context>

<must_haves>

- super_admin 登录后落到 role-aware module route；单域管理员直接落到自己的主工作面，多域管理员落到 `Overview`。
- 导航隐藏逻辑与 default landing 使用同一套 route metadata，不新增第二套 menu 条件判断。

</must_haves>
