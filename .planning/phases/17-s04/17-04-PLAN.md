---
phase: "17"
plan: "04"
type: "implementation"
---

<objective>
让 Mentor/Distribution 页面改用 shared http seam 并保住状态 UX
</objective>

<context>
Phase: 17 (s04) — Milestone: Admin 管理后台与后端多模块重构

S04 不能只交付 chrome；已有 `MentorAuditPage` 与 `DistributionStatsPage` 必须在新 auth seam 下继续工作。这个 task 把两个 data clients 和页面 prop model 切到 shared axios/auth context，保住 query params、error banners 和 current-admin context，使 downstream slices 在同一 transport/diagnostic 语义上继续扩展。
</context>

<must_haves>

- 已交付的 mentor/distribution workspaces 在新 shell 下继续 render，并保留 URL query context 与 operator-visible retry/error states。
- 两个页面继承统一 refresh/replay / fail-closed 语义，不再各自 hand-roll auth transport。

</must_haves>
