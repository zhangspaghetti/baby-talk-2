---
phase: "17"
plan: "01"
type: "implementation"
---

<objective>
补齐 ProLayout 基线、Warm Paper tokens 与 route catalog
</objective>

<context>
Phase: 17 (s04) — Milestone: Admin 管理后台与后端多模块重构

先把 `admin-web/` 从单文件 proof 提升为可扩展的 shell seed：补齐 `@ant-design/pro-components` / `@ant-design/icons` / `axios` 依赖、显式 TypeScript 配置、Warm Paper admin 子主题，以及 typed route catalog + placeholder pages。这个任务只建立前端 skeleton 与 metadata truth source，不引入第二套 runtime，也不把业务 CRUD 拉进 S04。
</context>

<must_haves>

- Route metadata 成为 S04-S07 的唯一路由/导航/权限来源，不再把 path/title/permission 散落在 `App.tsx` JSX 分支里。
- Theme tokens 符合 Warm Paper admin 子主题，去掉当前紫色视觉基线。

</must_haves>
