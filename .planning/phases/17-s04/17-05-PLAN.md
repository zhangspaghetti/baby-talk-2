---
phase: "17"
plan: "05"
type: "implementation"
---

<objective>
用 compose-backed Playwright 固化 auth/RBAC browser proof
</objective>

<context>
Phase: 17 (s04) — Milestone: Admin 管理后台与后端多模块重构

最后用真实 browser proof 把 S04 stop condition 钉住：基于 compose stack + real admin-api fixtures 写 `auth-and-rbac.spec.ts`，覆盖未登录跳转、super_admin landing、limited-admin hidden nav / forbidden deep link、forced access-token invalidation -> single refresh replay、refresh revoked -> logout。这里不做 client-only permission fake，所有 restricted-user fixtures 都走 S03 已交付的 admin RBAC APIs。
</context>

<must_haves>

- canonical browser proof file 是 `admin-web/tests/auth-and-rbac.spec.ts`，且基于真实 admin-api / RBAC fixture。
- spec 对 visible banner / URL / nav filtering / network refresh count 都有断言，失败时能直接看出是 auth、RBAC 还是 replay regression。

</must_haves>
