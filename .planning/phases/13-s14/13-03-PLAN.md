---
phase: "13"
plan: "03"
type: "implementation"
---

<objective>
重放 repo-root release closure，并在首个 red child surface 修 drift
</objective>

<context>
Phase: 13 (s14) — Milestone: Admin 管理后台与后端多模块重构
</context>

<must_haves>

- final proof 必须是 CI 同款 repo-root command，而不是 hand-assembled subset。
- 任何发现的 drift 都修在 truth owner（child verifier / runbook / docs），而不是通过复制 browser/Helm steps 在 S14 或 YAML 里“补绿”。
- 成功路径最终打印 `All M006/S14 release-closure verification steps passed.`，失败路径仍保留首个 red child gate 的 drill-down hint。

</must_haves>
