---
phase: "13"
plan: "02"
type: "implementation"
---

<objective>
把 CI 与 repo-root docs 收口到同一条 canonical release command
</objective>

<context>
Phase: 13 (s14) — Milestone: Admin 管理后台与后端多模块重构
</context>

<must_haves>

- workflow 继续直接运行 S14 gate，并在失败时保留 Playwright report/test-results artifact。
- root docs 与 runbook 继续共享同一条 repo-root final release command，并指回正确的 child verifier / runbook。
- deploy / release docs 继续维持 `admin-api` internal-only 边界，不把它写成 public entrypoint。

</must_haves>
